# GPU 调度面试 · TOP10 逐字口述稿

> 配套：《GPU调度面试题库-27题全解析.md》《GPU调度面试-覆盖度评估与补强题库.md》
> 选题意图：覆盖「必考 + 最能体现做过和背过的区别」的 10 题 —— Q5/Q8/Q9/Q17/Q20/Q22 + A1/A3/B1/B2
> 每份稿约 700–900 字，按正常语速（250 字/分钟）**约 2.5–3 分钟**，正好是面试官期望的回答时长。

---

## 使用说明

1. **不要逐字背诵**。记住骨架是：开场一句定调 → 三到四个"第一/第二"分点 → 收尾一句讲代价和落地。
2. 稿子里**用括号标了【停顿】和【钩子】**：钩子是故意留的口子，引导面试官往你熟悉的方向追问。
3. 每篇最后配了 **潜在追问的一句话回应**，防止追问时卡壳。
4. 练习方式：手机录音 → 回听 → 删掉口头禅 → 压到 3 分钟内，每篇练 3 遍即可形成肌肉记忆。

---

## NO.1｜Q5 多种 GPU 型号（A100/H100/L20/A10）如何做资源抽象和调度匹配

> 【开场】"我的思路不是按型号做匹配，而是按**能力**做匹配——先把硬件抽象成一组可以比较的参数，再让调度器在这个参数空间里做决策。【停顿】具体分四层。"

> **第一层，建立多维资源向量。** 每块 GPU 在上报的时候，带的不再只是一个名字，而是这样一个描述：架构代际是安培还是霍珀，FP16/BF16 算力峰值是多少，显存容量和带宽是多少，卡间互联是 NVLink 还是 PCIe，支持不支持硬件切分。调度器看到的是一个性能特征向量，而不是"A100"这个字符串。在这个基础上我定义"资源类"的概念，把性能相近的型号归成一类，用户提交任务时选的是类，不是具体型号。
>
> **第二层，约束引擎。** 支持两种匹配模式。一种是精确匹配，用户明确说我要 H100，那就走 nodeSelector 加型号标签，硬约束。另一种是模糊匹配，用户只声明"显存不低于 40G、算力不低于某个下限"，调度器在满足这个下限的卡里挑。这里的关键设计是**区分硬约束和软偏好**：模型和算子兼容性是硬约束，比如编译产物在霍珀上跑不了就必须卡死型号；而"尽量用好卡"是软偏好，只参与打分不参与过滤。
>
> **第三层，引入成本因子。** 不同卡的采购价和功耗差很多，我会给每种卡算一个"元每卡时"的虚拟成本，进打分权重。【钩子】这一步其实是带业务属性的。举个例子，一个只做数据预处理、算力需求很低的显存型任务，如果占了一张 H100，其实是把高价值资源浪费了。所以评分的时候，算力需求低的任务会被引导到 L20、A10 这类卡上，把 H100 留给真正需要它的大模型训练，这就是所谓的高低搭配。
>
> **第四层，特殊硬件特性适配。** 支持 MIG 的型号，在小任务场景下自动触发切分；NVLink 域内的连续卡，优先留给需要高频通信的多卡任务。
>
> 【收尾】"这套抽象的代价是**画像维护成本**——新卡进场要做基准测试和归档；如果偷懒直接用型号硬编码，每次上新卡都要改调度代码，系统很快就不可维护了。所以我宁愿前期多花力气做标准化。"

**追问预判 → 一句话挡住**：
- *"H100 上跑给 A100 编译的内核会怎样？"* → "PTX 前向兼容但 SASS 不向后兼容，重编译前会退化甚至失败——这正是我把代际作为硬约束的原因。"
- *"成本因子怎么标定？"* → "采购价按三年折旧 + 实测功耗电费，折算成每卡时；再按集群稀缺度做系数调节，稀缺卡系数更高。"

---

## NO.2｜Q8 GPU 调度系统的队列、配额、优先级和抢占怎么设计

> 【开场】"我会把它拆成四个互相制约的机制来设计：队列管组织、配额管上限、优先级管顺序、抢占管回收。四者必须**解耦**，否则业务一加需求整套就崩。【停顿】分别说。"

> **首先是队列。** 用层级队列树：**组织 → 团队 → 业务**，每个队列配自己的调度策略，可以是严格优先级，也可以是加权轮询。层级的好处是配额可以按父队列聚合，上级能给下级兜底。
>
> **第二是配额，这里我用双阈值。** 每个队列给一个 min 和一个 max。min 是保证额度，任何情况下这部分资源不能被别人抢走；max 是上限，防止一个团队吃满全集群。中间那段就是**可以弹性借用**的部分——我闲的时候给别人用，我要的时候收回来。
>
> **第三是优先级，要和配额解耦。** 任务级优先级只影响**队列内部的排序**，不能让高优任务直接越过队列配额去抢别的团队资源。这两个概念一旦混在一起，就会出现"某个团队写了几个高优任务就把整个集群占满"的情况，这在多租户环境里是不可接受的。
>
> **第四是抢占，这是最需要克制的机制。** 我只在一种情况下允许抢占：某个队列自己的保证额度没拿满，而资源被别人的**超发部分**占着，这时候才回收。回收的时候挑 victim 有三个原则：优先选可 checkpoint 的、优先级最低的、运行时间最短的——目的是把中断成本降到最低。还有一个容易漏的点：**reserve one**，抢占出来的资源要先给发起方预留，否则马上又被第三个任务抢走，形成抢占循环。
>
> 【收尾】"真正落地时最大的坑是**抢占风暴**。所以我会给抢占加限流和冷却期，同时给被抢占方发'欠条'，下次资源释放时优先偿还——这样长期来看低优任务不会被饿死。"

**追问预判**：
- *"Volcano 和 Yunikorn 你怎么选？"* → "要 Gang 和插件生态选 Volcano；要企业级层级配额和强抢占语义选 Yunikorn；只要排队和准入、不想替换原生调度器就上 Kueue。"

---

## NO.3｜Q9 多团队共享集群，如何保证公平性

> 【开场】"公平这个词要先定义清楚，我的定义是：**长期来看，每个团队拿到的资源接近它的权重份额，而不是某一瞬间相等**。【停顿】围绕这个定义我设计了四条机制。"

> **第一条是保底。** 每个团队有 min guarantee，这是硬承诺。资源紧张的时候，只回收超出保证额度的部分。这样即便最坏情况，每个团队都有确定的产出能力，不会出现某个团队连续三天排不上队的情况。
>
> **第二条是按需回收与比例公平。** 真正的难点在于 min 和 max 之间的弹性部分怎么分。我的做法是按"超发比例"来回收——谁超得越多，回收的时候权重越大。这里我一般引用 **DRF，主导资源公平**：不能只看 GPU 卡数，因为有的团队是显存密集、有的是算力密集。DRF 比较的是每个租户的**主导资源占比**——占自己那份比例最高的那个维度，谁的主导份额大谁先让。这比单纯按卡数公平要准确得多。
>
> **第三条是欠条和记账式归还。** A 借了 B 的资源，B 需要的时候必须优先归还，这是把所有借用行为记成账。这样"借用"就不是白占便宜，而是有借有还。
>
> **第四，我觉得很多团队忽略的一点：可解释性。** 公平性最后一定会变成一个沟通问题，而不是技术问题。所以我把每个团队的"申请量、保底量、实到量、超发量、欠条"做成可视化的账本公开出去。【钩子】当团队来质疑的时候，我可以直接把这张账本拉出来跟他逐笔对，而不是口头解释调度器很复杂。
>
> 【收尾】"最后补一句公平性的敌人：**虚报需求**。所以我一定会配套做配额定期校准，把长期不用或者使用效率极低的配额收回来。"

**追问预判**：
- *"DRF 具体到 GPU 怎么算主导资源？"* → "把 GPU 卡数、显存、CPU 各自按集群总量归一，取每个租户占比最大的那一维作为它的主导份额，比较所有租户的主导份额。"

---

## NO.4｜Q17 在线推理和离线训练混部，怎么设计隔离和弹性

> 【开场】"混部的本质是拿**空闲算力**做交换，风险是干扰。所以我从来不说'能不能混'，而是回答'用什么级别的隔离去混'。【停顿】我分隔离、弹性、兜底三层来看。"

> **第一层是隔离，这是能不能混的前提。** 要拆成三个维度分别谈。显存维度：要么用 MIG 做物理隔离，要么用 vGPU 的显存硬上限，绝不能让离线任务把在线推理的显存挤爆，那是直接 OOM。算力维度：MPS 的算力百分比限制，或者时间片配额，保证在线那边至少能拿到它需要的份额。故障维度：这是我最看重的一点，MIG 提供真正的故障隔离，一个切片上的 Xid 不会波及另一个切片；而 MPS 和纯时间片共享，一个租户的驱动错误会把整块卡拖下水，所以**跨租户、有合规要求的场景我只认 MIG**。
>
> **第二层是弹性策略，也就是潮汐。** 在线服务的 QPS 是白天高夜间低，训练是反向的，这两条曲线天然互补。白天把绝大部分卡给在线，夜间把在线缩容后空出来的卡借给训练。关键是**早高峰之前要有一个驱逐窗口**——不是等 QPS 涨上来才被动抢占，而是提前按预测曲线把训练任务有序退出，给它们留 checkpoint 的时间。这个设计能省掉一大堆客服投诉。【钩子】
>
> **第三层是兜底。** 即便做了预测，也会有突发。突发时走秒级抢占：SIGTERM 通知 → 给 grace period → 超时强杀 → 释放显存 → 在线 HPA 拉起。同时要有自动熔断：一旦监控到在线 P99 超过阈值，自动把混部卡上的离线任务全清掉并回滚，不用等人介入。
>
> 【收尾】"上线路径上我会很保守：先灰度 5% 的卡，跑一周干扰压测，量化双方的退化幅度——在线 P99 涨了多少、离线吞吐降了多少——在 SLO 之内才逐步放量。混部是收益最大的手段，也是最容易翻车的手段，所以我宁可慢。"

**追问预判**：
- *"混部之后在线 P99 抖了怎么办？"* → "先看是不是算力争抢，收紧 MPS 配比；再看是不是 PCIe/NUMA 争抢，换 NUMA 亲和绑定；都不行就退回独占池，不为了利用率指标牺牲 SLA。"

---

## NO.5｜Q20 集群利用率指标体系怎么设计，避免被单一 Util 误导

> 【开场】"这个问题我有比较强的观点：**只盯着 nvidia-smi 那个 Util 做优化，大概率会做出错误的决策。**【停顿】因为那个数字的定义本身就有误导性。"

> 先说为什么。GPU Util 的含义是"采样周期内有没有 kernel 在跑的时间占比"，它是一个**占空比**，不是算力强度的度量。一个在里面空转等待数据、或者只占几个 SM 的小 kernel，照样能把 Util 撑到 100%。所以经常出现一种荒谬的情况：Util 百分之百，但有效算力只用了百分之二十。
>
> 所以我的指标体系分四层。**第一层是强度层，用来修正 Util 的缺陷**：SM 活动率、occupancy、显存带宽利用率、kernel 实际执行时间占比。这四个里我最看重显存带宽利用率，因为它和真实计算强度相关性最高。
>
> **第二层是产出层，这是北极星。** 定义为每卡时的业务产出——训练看 samples/s 或者 tokens/s，推理看达标 QPS。这一步是把"资源指标"升级成"价值指标"。它的好处是自然兼顾两个方向：既要提高产出，又要少占卡时，而且这个口径业务方认。
>
> **第三层是效率层**，看资源被浪费在哪：分配率和实际使用率之差（申请了不用）、排队时间和运行时间之比、碎片率。这一层直接告诉我们优化空间有多大。
>
> **第四层是健康层**：Xid 错误率、降频卡比例、拓扑碎片率、混部干扰系数。
>
> 【收尾】"落地上有两个经验。一是采集层用 DCGM 而不是轮询 nvidia-smi，开销低得多也准得多；二是**报表必须同时展示 Util 和有效利用率两列**——这是在训练整个团队看对的数字，指标上线本身就是一个组织推动的过程，不只是技术活。"

**追问预判**：
- *"有效利用率到底怎么算？"* → "我一般用 min(SM 活动率, 显存带宽利用率) 或者 kernel 强度的加权值，具体公式不重要，关键是被业务方认可并且可复核、可追溯。"

---

## NO.6｜Q22 MIG / MPS / Time-Slicing 三种共享方式怎么选型

> 【开场】"这三个方案不是好坏之分，而是**隔离强度和灵活性之间的取舍**，我一般从三个隔离维度去比：显存、算力、故障。【停顿】先给结论，再讲理由。"

> **MIG 是硬件级隔离。** 卡被切成若干实例，显存是物理划分的，算力是固定切片，甚至连错误隔离域都是独立的——一个实例上的 ECC 错误不会影响另一个。代价是**切分方案是预先定义的**，而且要改就必须排空这张卡上的所有任务，重配置成本很高，而且切片之间没有 NVLink。所以它的适用边界很清楚：**多租户生产推理、有合规或稳定性硬要求、显存需求稳定的场景**。
>
> **MPS 是进程级服务化共享。** 所有进程通过一个 MPS server 提交 kernel，可以设每个进程的活动线程百分比来限制算力。它的最大优点是**算力配比可以动态调整**，所以适合 QPS 波动的场景。但显存是不隔离的，理论上一个进程可以超用把别人挤掉；故障也不隔离，MPS server 挂了大家一起遭殃。所以适用的是**同租户下多进程聚合、需要算力 QoS 又要求弹性**的场景。
>
> **Time-Slicing 是纯时间片轮转。** 实现最简单，任何卡都支持，K8s 里配个插件就行。但它没有任何 QoS：一个任务如果跑一个长 kernel，其他任务就要等一整个时间片，延迟可能被放大一个数量级。所以我只在**开发测试、低优先级填充、不怕干扰**的场景用它，绝不用于生产在线推理。
>
> 【收尾 · 给一张总结】用一句话记就是：**要硬隔离选 MIG，要弹性算力选 MPS，要简单便宜选 Time-Slicing。** 实际生产里我通常是组合在用：VIP 租户跑 MIG，普通推理用 MPS 带算力配比，开发调试池用时间片，三种池并存，靠队列和标签路由。"

**追问预判**：
- *"MPS server 单点怎么办？"* → "节点 agent 做健康探活，挂了自动重建并把在本卡上的任务按优先级重建，同时把该卡标记降级一段时间不给新任务。"

---

## NO.7｜A1 Scheduler Framework 扩展点，以及 GPU 插件怎么落地

> 【开场】"GPU 调度增强我不会去改 kube-scheduler 源码，而是基于 Scheduler Framework 写插件，这样上游升级可以直接跟。【停顿】我把和 GPU 相关的逻辑按扩展点拆开说。"

> 一次调度周期的执行顺序是：PreEnqueue → PreFilter → Filter → PostFilter → PreScore → Score → NormalizeScore → Reserve → Permit → PreBind → Bind → PostBind。
>
> 我在这些点上的落地分别是：**Filter** 做硬约束——显存够不够、型号代际对不对、拓扑能不能满足，比如说要 8 卡 NVLink 域的任务，在这个点就把不满足的节点刷掉。**Score** 做偏好打分——binpack 分让它尽量紧凑、成本分让它往便宜的卡上走、亲和分让它靠近它的数据。**Reserve** 这一步很关键，它在 Permit 和 Bind 之间把资源锁住，避免同一张卡被两个 Pod 同时选中。
>
> **Permit 是实现 Gang 的核心**：一个训练任务的 8 个 Pod，必须全部通过过滤、全都预留成功，才在 Permit 统一放行；只要有一个不到位或者超时，就全部回滚释放。这是防"部署一半"和死锁的关键闸口。**Bind** 阶段我还会扩展它做一件原生不做的事：**把具体分配的 GPU UUID 写进 Pod 的 annotation**，因为 K8s 原生只知道"一块卡"这个计数，不知道是哪一块，而 device plugin 需要知道确切编号。
>
> 多个 Score 插件的聚合方式是：各自返回 0 到 100 的分，平台负责把每个插件的分数做归一化之后按权重加权求和，权重写在调度器配置里，可以按不同的 profile 切——比如训练 profile 偏重拓扑分，推理 profile 偏重亲和和延迟分。
>
> 【收尾】"这里我踩过的坑是**所有逻辑都堆在 Filter**。Filter 是串行执行的，节点越多越慢；能放到 Score 或者预计算的东西一定要挪出去，否则大集群下调度延迟会失控。"

**追问预判**：
- *"多插件打分冲突怎么办？"* → "靠权重配置，并且对同一份 workload 做离线回放对比，用真实调度结果反推权重，而不是拍脑袋定。"

---

## NO.8｜A3 如何避免两个 Pod 同时被调度到同一张 GPU

> 【开场】"这个问题本质上是**调度并发正确性**，GPU 场景比原生 K8s 更难，因为 GPU UUID 不是 K8s 原生资源，没有事务保护。【停顿】我分原生机制和自定义资源两部分讲。"

> 先看 K8s 原生的做法，它用的是**乐观并发加假定**，而不是加锁。调度器在决定把 Pod 放到某个节点之后、真正 Bind 之前，会先做一次 **Assume Pod**：把这个 Pod 假装已经放上去了，写进本地的 SchedulerCache。这样同一个调度周期内、后处理的 Pod 在打分的时候就能看到它占用的资源。接下来 Reserve 扩展点把这份占住在 Permit 到 Bind 的整个过程中锁住。这里面有个细节：assumed 状态的 Pod 是有 TTL 的，如果一段时间内 informer 没确认它真的存在，缓存会把它清掉，防止幽灵占用。
>
> 真正的冲突是在 **Bind 阶段**暴露的——API server 那边的写入如果发现节点资源不够或者 nodeName 已经被别人占先，Bind 失败，调度器就把 Pod 重新丢回队列走下一轮。这就是乐观并发：**冲突少则收益大，冲突多则退化重试**。
>
> 到 GPU UUID 这一层就没有原生保护了，得自己做。我的做法是把 GPU UUID 的分配也当成一个需要 CAS 保护的资源：要么把分配结果写进 CRD 状态做事务性更新，要么在 SchedulerCache 里对同一节点的 GPU 分配加互斥，确保一个调度周期内同一 UUID 只会被发出去一次。【钩子】更保守的做法是按节点维度加锁，代价是并行度下降，我一般只在竞争激烈的热点节点上这么做。
>
> 【收尾】"所以总结下来就是三层防线：Assume Pod 保证周期内可见、Bind 的乐观冲突检测兜底、GPU UUID 自己做原子分配。少任何一层都会出现超卖。"

**追问预判**：
- *"乐观并发冲突率高会怎样？"* → "表现为 Bind 失败率上升、调度吞吐下降。此时按资源热度做哈希分区，把热点节点的调度串行化，用局部串行换全局吞吐。"

---

## NO.9｜B1 多机多卡训练的木桶效应与慢节点如何检测处置

> 【开场】"这是大规模训练集群**最痛的一个问题**，因为它不会体现在任何单卡指标上——每块卡的 Util 看起来都很漂亮，但整体吞吐就是上不去。【停顿】根源在于集合通信是同步的。"

> 一次 AllReduce，所有参与方必须等最慢的那一个，所以一个节点慢一步，整个 step 就慢一步。这个最慢者可能是 GPU 本身、也可能是网络链路、也可能是这台机子的 CPU 或者磁盘拖了数据加载。
>
> 检测这块我通常用四个信号交叉验证。第一是**每 step 的耗时分布**，正常情况下非常稳定，一旦出现长尾就是通信侧有异常。第二是 NCCL 的超时和卡死日志，这是最直接的证据。第三是网络计数器——IB 或 RoCE 端口的丢包、重传、credit stall，如果是 incast 或者 PFC 风暴引发的拥塞扩散，这里会先报警。第四是节点级 GPU 利用率的**同步性**，正常训练的卡 SM 曲线几乎是同一个形状，慢节点会出现明显的相位滞后。再叠加 DCGM 的 Xid 和温度、时钟降频数据。
>
> 处置上我做分级：**一级告警加 cordon**，不再往这台机子上下新任务；**二级不允许承接长跑训练**，只给短任务；**三级自动 drain 迁移任务**并生成报修单。更高级一点的是做**节点健康画像和亚健康预测**——不是等它真的慢了才处理，而是看到 ECC 计数趋势上升、温度墙频繁触发、重传率爬坡，就提前把它摘下来。
>
> 【收尾】"这里我要强调一个容易被忽略的设计点：**同一次训练的多个 replica 要跨故障域打散**。如果把一个千卡训练的所有节点都堆在同一个机柜，一次掉电或者一台交换机故障就是全军覆没，修复成本远大于局部通信收益带来的那点提升。"

**追问预判**：
- *"RoCE 网络怎么防止多任务互相打爆？"* → "PKEY 分区按 job 隔离通信域，配 ECN 加 DCQCN 做拥塞控制，同时用带宽 QoS 限速，避免某个大通信量任务把整个 fabric 拖死。"

---

## NO.10｜B2 不同并行策略下的拓扑感知放置怎么做

> 【开场】"这是我认为最能区分候选人水平的一道题。很多人的答案是'多卡任务尽量放同一台机器'，但**真正决定性能的是通信模式**。【停顿】我会按并行策略分别设计放置约束。"

> **张量并行 TP**，通信量最大而且延迟最敏感，AllReduce 在每一层的正反向都要做。所以它必须满足最强的约束：**同一台机器、同一个 NVLink 域、最好是连续的卡**。一旦跨机，训练吞吐会被PCIe和网络的延迟直接压垮，这个收益差是数量级的。
>
> **流水线并行 PP**，它的通信是相邻 stage 之间的点对点传输，数据量比 TP 小，延迟敏感度也低一些。所以它可以跨机，但要求跨机链路是高带宽低延迟，也就是要走 RDMA。
>
> **数据并行 DP**，通信是梯度 AllReduce，频率是每 step 一次。它的约束是最优性问题而不是可行性问题——尽量放在同一个 leaf switch 下面，最好不出同一个 pod；一旦跨 spine，流量翻倍，会和集群其他任务抢带宽。
>
> **专家并行 EP**（MoE 场景），它的通信模式是 all-to-all，突发流量很大，对**网络对称性**要求特别高，同时还存在专家到卡的映射问题，以及负载失衡带来的新形态 straggler。
>
> 所以落到调度器上，我的做法是**让任务带并行策略标签**，调度器看到这个标签之后翻译成分层的拓扑约束：先按 NVLink 域过滤，再按 leaf switch 打分，最后才是节点打分。也就是"先看通信域，再看资源量"。【钩子】这也是为什么我在做资源抽象（就是前面那套多维资源向量）时，把互联拓扑作为 GPU 的一个属性维度带上——因为它最终决定任务的放置弹性和可放位置的数量。
>
> 【收尾】"补充一句代价：拓扑约束越强，任务的**可放置性越差**，排队时间越长。所以我不会死守约束，而是按集群紧张程度动态放宽——空闲时严格要求放到同一个 NVLink 域，紧张时降级成'尽量同机、允许跨 leaf'，并把这个降级记录到任务事件里，事后可审计。"

**追问预判**：
- *"拓扑约束导致排队变长怎么办？"* → "做准入可见性，让用户知道自己要等多久，并提供'放宽拓扑'选项让用户自己权衡时间和速度，把选择权交出去。"

---

## 附：临场通用技巧

1. **开场第一句一定先给结论和框架**（"我分三层看"），面试官马上会知道你有结构，愿意听下去。
2. **每道题结尾必须讲代价和落地路径**，这是专家和工程师最大的区别——只讲优点的答案一定会被追问到崩。
3. **不会的题不要硬答**，直接说"这块我没有一线实操，但我理解的原理是……"，诚实比编造安全得多，而且通常面试官会换个角度再问。
4. **主动埋钩子**：在最熟悉的点上留一句"（这个策略）涉及到XXX，我可以细讲"，引导面试官往你的强项追问，把面试节奏握在自己手里。
5. **数字要敢给**：调度延迟 P99、碎片率、超卖比例、集群利用率提升幅度，哪怕给区间也比完全不说强。
