# GPU 虚拟化 / 切分 / 多租户隔离 · 14 题整理与扩展

> **来源**：用户上传的 9 张面试题库截图（第 2 套，与工作区既有《GPU调度面试题库-27题全解析.md》**非同一套题**）。
> **本套定位**：偏 **AI Infra 基础设施工程**——K8s 云原生调度机制 + GPU 虚拟化/切分 + 多租户隔离 + 算力防饿死 + 显存超卖 + 故障隔离，技术颗粒度比「27 题（调度策略与资源运营）」更深、更贴近驱动/运行时层。
> **结构**：① 图内 14 题逐题整理（考察点 + 答题要点 + 追问预判）→ ② 图内自带 Q12 完整示范解析 → ③ 按主题**扩展**的新增考题（42 题）→ ④ 答题方法论与负面示例 → ⑤ 与既有资料的衔接建议。

---

## 〇、题库总览

| 部分 | 题号 | 主题 | 技术层 |
|---|---|---|---|
| **第一部分：Kubernetes 与云原生容器调度核心机制** | Q1–Q8 | 调度流水线局限、扩展方式、拓扑感知、细粒度协同、Gang、层级队列、调度器选型、XID 故障 | 调度器 / 控制面 |
| **第二部分：GPU 虚拟化、切分与多租户隔离** | Q9–Q14 | 四种切分技术本质、MIG 隔离与 QoS、显存拦截、算力防饿死、显存超卖、CUDA Error 隔离 | 驱动 / 运行时 / 硬件 |

**出题规律**：第一部分把「K8s 原生能力边界」逐一去顶——凡是原生做不好的（整数卡、无 Gang、无拓扑、无抢占、无共享），就问你怎么补。第二部分则把「共享一张卡」这件事从最硬（MIG）到最软（Time-Slicing）拆开，问你的隔离强度认知与工程落地细节。

---

# 第一部分：Kubernetes 与云原生容器调度核心机制

## Q1. Kubernetes 默认调度器的调度流水线（Queue → Filter → Prioritize → Bind）在扩展支持 GPU 资源时存在哪些局限？

**考察点**：是否真正理解默认调度语义与 GPU 资源特性之间的鸿沟（而不是背调度流程）。

**答题要点**：分「资源模型、调度语义、设备绑定、规模性能」四个层面的局限。

1. **资源模型太粗**：默认把 GPU 当成「一块不可分割的整数计数」（`nvidia.com/gpu: 1`），没有显存、算力、型号代际、互联拓扑等维度，卡与卡之间被视为同质。
2. **缺乏成组语义**：默认是「逐个 Pod 调度」，没有 Gang/Coscheduling 概念。分布式训练要求 N 个 Pod 原子获得资源，原生会出现"分配一半卡住"（partial scheduling / 资源死锁）。
3. **不感知拓扑**：Filter 只判断"资源够不够"，不看 NVLink 域、PCIe/NUMA 亲和、跨机 leaf-spine 距离，多卡任务可能被拆到通信最差的卡位上。
4. **抢占语义不匹配**：K8s 的 PriorityClass 是配合 `preemption` 做「整 Pod 驱逐」的，而 GPU 内核任务是**非抢占式（run-to-completion）**，驱逐代价极高——不能靠"杀任务"来保证优先级。
5. **无法表达共享/超卖**：整数卡、独占分配，无法表达 MIG 切片、vGPU 显存配额、时间片共享、显存超卖等细粒度形态。
6. **调度与设备绑定脱节**：调度器只负责"节点级"分配，具体是哪块 GPU UUID 由 kubelet 侧 Device Plugin 在 Bind 之后决定，调度器看不到、也管不了，天然存在信息不对称与超卖风险。
7. **大规模性能**：Prioritize 阶段对全部节点打分，节点上万时单次调度延迟上升；且默认调度循环是「单 Pod 串行」为主，吞吐受限。

**追问预判**：
- *「你说的这些局限，社区怎么解的？」*→ 成组用 Volcano/Kueue，拓扑用 NUMA-aware/topology plugin，共享用 Device Plugin + 自定义调度插件，细粒度用 DRA（Dynamic Resource Allocation）。
- *「PriorityClass 为什么救不了 GPU 优先级？」*→ 因为 GPU 任务不可被硬件抢占；PriorityClass 只能决定**谁先被调度**和**谁被驱逐**，不能决定"正在跑的低优任务给高优让出算力"。

---

## Q2. 在 K8s 中扩展调度 GPU 资源时，使用 Extended Resources、Device Plugin 机制与定制 Scheduler Plugin（基于 Scheduling Framework）各自的优缺点是什么？

**考察点**：K8s GPU 扩展的三条技术路线是否拎得清，能否做选型。

**答题要点**：

| 方式 | 本质 | 优点 | 缺点 |
|---|---|---|---|
| **Extended Resources（扩展资源）** | 用 `node status.capacity/allocatable` 声明非 K8s 原生资源（如 `nvidia.com/gpu`），Pod 用 `resources.requests/limits` 申请 | 零开发成本，原生调度器直接支持，配额、亲和、binpack 全复用 | **只能是整数**；无法表达显存/算力维度；不能表示拓扑；无法做设备级健康感知；本质只是"计数器" |
| **Device Plugin 机制** | kubelet 侧插件，实现 `ListAndWatch / Allocate / GetPreferredAllocation / PreStartContainer`，上报设备并以扩展资源形式暴露 | 官方标准，负责设备的发现、分配、健康上报、容器内注入（挂载 `/dev/nvidia*`、设环境变量）；是 GPU 可用性的**基础** | 只解决"设备怎么给到容器"，**不解决"调度决策"**；仍是整数粒度（除非上报"切片"当独立设备）；调度器与设备分配之间是两阶段、非事务 |
| **定制 Scheduler Plugin（Scheduling Framework）** | 在 PreFilter/Filter/Score/Reserve/Permit/Bind 等扩展点写自定义插件 | 可做**显存/算力/型号/拓扑的细粒度约束与打分**，可做 Gang（Permit）、binpack、成本、抢占；不改 kube-scheduler 源码，跟得上上游版本 | 开发与维护成本高；需要自己保证并发正确性（Reserve/Assume）；插件越多调度越重；与 Device Plugin 需要"约定"如何表达资源 |

**结论口径**：三者不是替代关系，而是**叠加关系**——Device Plugin 做设备上报与注入是底座，Extended Resources 是它的天然表达形式，Scheduler Plugin 承载"更聪明的决策"。要上细粒度/拓扑/Gang，就必须有 Scheduling Framework 插件。

**追问预判**：
- *「Device Plugin 上报的资源为什么必须是整数？想报半张卡怎么办？」*→ 原生 extended resources 语义是整数计数；变通办法是**把一张卡切成 N 个逻辑设备单元**上报（如把卡切成 10 份 → `nvidia.com/gpu: 10`），或用独立资源维度（HAMi 风格 `nvidia.com/gpumem`、`nvidia.com/gpucores`）。
- *「Extender（HTTP 回调）和 Plugin 的区别？」*→ Extender 是外挂 HTTP 服务，性能差、易成瓶颈、配置静态；Plugin 是进程内插件，是当前主流，Extender 已被框架插件大部分取代。

---

## Q3. 如何设计和实现 GPU 拓扑感知调度？在同一节点内如何结合 NVLink 与 PCIe 拓扑关系进行最优卡位分配？

**考察点**：是否理解"卡间通信带宽决定训练效率"，以及拓扑如何建模、进调度器。

**答题要点**：

1. **拓扑建模（把硬件建成图）**：
   - 节点内：把 8 张卡按互联关系建成图。`nvidia-smi topo -m` 给出的是 `NV#`（NVLink 桥数）、`PIX`（同 PCIe switch）、`PHB`（同 PCIe host bridge）、`SYS`（跨 NUMA socket，走 QPI/UPI）等等级。
   - 典型节点形态：(a) 8 卡 NVSwitch 全互联（H100/A100 SXM），任意两卡对等高速；(b) 双 NUMA，每 NUMA 挂 4 卡，NVLink 域内 4 卡全互联、跨 NUMA 走 SYS 慢路。
   - 节点间：leaf-spine 网络、Rail-optimized 布线（每张网卡对应固定 GPU 号，如 GPU i ↔ NIC i），决定跨机 AllReduce 的等价带宽。
2. **约束表达**：把拓扑抽象成 `topologyKey`（如 `nvlink-domain`、`numa-node`、`rail`），GPU 作为带这些标签的可分配资源；Device Plugin 上报时附带拓扑属性（或写 device topology annotation）。
3. **分配策略**：
   - **连续性优先**：多卡任务优先分配**连续编号、同 NVLink 域**的卡；
   - **域打包**：2 卡任务不跨 NUMA；4 卡任务填满一个 NUMA；8 卡任务要求整机全互联；
   - **跨机最小化**：能整机满足就绝不分跨机，必须跨机时按 leaf switch 距离就近排布。
4. **落地**：Filter 阶段做硬约束（不满足拓扑的节点直接淘汰），Score 阶段做软偏好（拓扑紧凑度打分）；对"要 8 卡 NVLink 域"的任务，本质是要求节点有空闲的**整域**。

**追问预判**：
- *「拓扑约束太严，排队变长怎么办？」*→ 动态放宽：空闲时严格、紧张时降级为"尽量同机/允许跨 leaf"，降级记入任务事件可审计；并给用户"放宽拓扑换更快启动"的选项。
- *「怎么感知 NVLink 降级？」*→ DCGM/`nvidia-smi nvlink` 采集各 link 的 CRC/吞吐，异常时把该卡从"高速域"降级，放置时避开。

---

## Q4. K8s 原生只支持整数卡调度，在实现多租户 GPU 显存与算力细粒度切分（如 0.5 卡、显存配额）时，调度层与底层运行时的如何协同？

**考察点**：调度器（控制面）与 kubelet/Device Plugin/运行时（数据面）的职责分工与信息流是否讲得清。

**答题要点**：核心是「**两层解耦、一次对齐**」。

1. **表达能力层（怎么描述 0.5 卡）**：
   - 路径 A：把卡切成 N 个**逻辑设备**，Device Plugin 上报 `nvidia.com/gpu: N`，Pod 申请 1 个 = 1/N 卡（粗糙，粒度固定）。
   - 路径 B（主流）：**自定义多维资源**——`nvidia.com/gpumem: 8192`（MiB）+ `nvidia.com/gpucores: 50`（百分比），Pod 同时声明两者，调度器按二维约束匹配（HAMi / 腾讯 qGPU / 阿里 cGPU 均此思路）。
   - 路径 C（未来）：**DRA** —— 用 ResourceClaim/ResourceClass 声明结构化设备需求。
2. **调度层（webhook + 自定义插件）**：
   - Mutating Admission Webhook 把用户写的 `nvidia.com/gpumem` 请求做**准入校验与转换**，并选择节点（或注入 nodeSelector）；
   - Scheduler Plugin 在 Filter 里校验节点剩余可实现显存/算力，Score 里做紧凑度/碎片打分，**Bind 前把分配结果写回 Pod annotation**（记录要绑哪块卡、多少显存）。
3. **运行时层（Device Plugin + 节点 Agent）**：
   - kubelet 调 `Allocate()` 时，插件按 annotation 里指定的卡执行**真实隔离动作**：设置 `CUDA_VISIBLE_DEVICES`、挂载 MIG 实例、或注入 LD_PRELOAD 拦截库限制显存/算力；
   - 节点侧维护 `Device → Pod` 的分配账本，负责回收与状态恢复。
4. **协同关键点（三个"必须对齐"）**：
   - **调度结果 ↔ 实际绑定必须原子**（否则两轮调度把同一份显存发给两个 Pod）；
   - **节点可分配量 ↔ 实际切分能力必须对齐**（Device Plugin 上报的 allocatable 要随切分状态动态更新）；
   - **资源释放 ↔ 节点状态回收必须及时**（Pod 删了要归还显存账本）。

**追问预判**：
- *「调度时怎么知道节点还剩多少可切显存？」*→ 由节点 Agent 周期性上报 `已分配 / 总容量 / 可再切分余量`，Device Plugin 据此更新 allocatable。
- *「如果调度器认为够、运行时却分不出来怎么办？」*→ 双保险：调度侧做保守估计（预留水位），运行时 Allocate 失败则把 Pod 标记失败并回到队列，同时上报事件用于校准。

---

## Q5. 针对大模型分布式训练（如 64 卡、128 卡），如何实现 Gang Scheduling（组调度）与 Coscheduling？如何防止资源死锁（Deadlock）？

**考察点**：分布式训练的原子调度这一"专家分水岭"问题，能否讲清死锁的成因与解法。

**答题要点**：

1. **为什么必须 Gang**：一个训练任务 = N 个 worker，必须**全部同时就位**才能启动（集合通信会互相等待）。若只起了 90%，剩下的等资源，已起的 90% 空占资源干等 → 集群吞吐塌陷 + 资源死锁。
2. **实现载体**：
   - **Volcano**：`PodGroup` CRD 声明最小成员数 `minMember` + `minResources`，`gang` 插件在 Pod 逐个 Filter/Reserve 后，未达 `minMember` 前**不放行、只占位**，达到后统一 Permit → Bind。
   - **Coscheduling（scheduler-plugins）**：`PodGroup` + `Permit` 扩展点，Pod 达到 `spec.minMember` 才放行，否则超时后回滚已分配资源。
   - **Kueue**：做的是**准入排队**——工作量不超过配额时才允许创建 Pod，从源头避免"起一半"。
3. **防死锁（核心）**：
   - **超时回滚**：Permit 等待超时（如 5 min）则**释放已占资源**、整组退回队列，避免"半成品"长期占坑——这是最有效的一招；
   - **准入控制**：集群剩余资源不足以支撑该组最小规模时，**不接纳**，宁可不开始也不半启动（Volcano 的 `minResources` 起此作用）；
   - **优先级 + 抢占组内一致**：高优 PodGroup 可整组抢占低优，避免多组互相错位等待形成**环形等待（circular wait）**；
   - **资源规整/槽位化**：把碎片归整成大块（按 8 卡节点槽位分配），从"熵"层面降低死锁概率；
   - **等待图检测（进阶）**：把"谁在等谁"建成有向图，检测环并打破（选代价最小的一组回滚）；
   - **Backlog 限流**：大量训练任务同时重调度时，对重调度速率限流，防止"重试风暴"互相踩踏。

**追问预判**：
- *「超时回滚会不会导致任务永远启动不了？」*→ 会形成"饥饿"，所以回滚要配合 **Aging（等待越久优先级越高）** 或预留窗口，保证最终能拿到整组资源。
- *「Gang 和弹性训练冲突吗？」*→ 不冲突：严格 Gang 用于"开始即锁定 world_size"，弹性训练则允许在 min–max 区间内伸缩，调度器要能识别弹性区间并做 grow/shrink 而非硬杀。

---

## Q6. 在海量 GPU 作业排队场景下，如何设计多租户层级队列（Hierarchical Queue）、多维度配额（Quota）与弹性借调/抢占机制？

**考察点**：多租户平台的核心治理能力——四套机制能否解耦。

**答题要点**：

1. **层级队列**：树形结构 `组织 → 团队 → 项目`；每个队列绑定调度策略（严格优先级 / 加权轮询 / 公平份额 / DRF）；父子队列配额可继承与聚合，父队列为子队列兜底。
2. **多维度配额**：不止按卡数，还要按 **GPU 数 / 显存 / CPU / 内存 / 自定义维度（如某型号卡数）** 设 min/max；支持**分时配额**（白天/夜间两套模板，匹配业务潮汐）。
3. **弹性借调（借用）**：
   - 队列空闲资源可借给他人（`min` 保底不借出，`min~max` 之间可借）；
   - 借出后要**记账（欠条 / credit）**，出借方需要时按优先级归还（reclaim）。
4. **抢占机制**：
   - 只在"高优队列保底额度未满足、且资源被他人**超发部分**占用"时触发；
   - victim 选择原则：优先级最低、可 checkpoint、运行时间最短（降低中断成本）；
   - **reserve one**：抢占所得先给发起方预留，防止被第三方瞬间夺走，形成"抢占循环"；
   - **抢占限流 + 冷却期**：防止抢占风暴。
5. **避免跨子树死锁**：多层级借调时，只允许"自底向上回收本子树内的借用"，不跨父节点互相借调，从结构上避免环形等待。

**追问预判**：
- *「层级配额怎么算 min/max？」*→ 父队列 min = 子队列 min 之和（硬保底量），父 max 由集群总量与兄弟队列共享决定；实现上常用"配额模板 + 优先级"而非纯数值。
- *「怎么防止某个团队虚报配额？」*→ 按实际使用率校准配额（长期低效/闲置则回收），并对超用部分做内部成本分摊。

---

## Q7. Volcano、Yunikorn 以及 Kubernetes 原生调度器，在大规模 AI 训练和推理场景下的调度吞吐与特性差异有哪些？

**考察点**：技术选型判断力（能否给出"什么场景选谁"的结论）。

**答题要点**：

| 维度 | K8s 原生 kube-scheduler | Volcano | Yunikorn |
|---|---|---|---|
| 定位 | 通用、逐 Pod、单集群 | 面向 **HPC/大数据/AI 批处理**，CNCF 毕业 | 源自 Hadoop YARN，面向**多租户层级队列**，支持 K8s+YARN |
| 组调度 | ❌（需自研或装 coscheduling 插件） | ✅ **PodGroup + Gang 核心能力** | ✅ 以"应用"为调度单元，天然组调度 |
| 队列/配额 | 弱（需 ElasticQuota/自研） | Queue + 多级队列 + **能力丰富** | ✅ **层级队列 + 强配额 + 抢占**语义最完整 |
| 抢占 | 基于 PriorityClass，语义简单 | 队列内公平份额抢占 | 队列/应用级抢占，语义更强 |
| 插件生态 | Scheduling Framework 插件 | 大量内置 plugin（binpack / drf / proportion / numa-aware / device-share / task-topology / tdm 等） | 队列策略丰富，AI 特定插件相对少 |
| 吞吐 | 中等，大集群需调优（percentageOfNodesToScore） | 高（批量调度、成组处理） | 高（应用级批量、YARN 血统） |
| 推理场景 | 通用够用（配合 HPA/推理引擎） | 偏批处理，推理可用但非强项 | 多租户共享场景强 |
| 适用结论 | 基础调度、在线业务 | **自研 GPU 调度平台的主流底座** | **企业多租户强配额、K8s+YARN 混合** |

**补充**：**Kueue** 常被拿来与二者对比——它做的是 K8s 官方「**作业排队与配额准入**」（ClusterQueue/LocalQueue/ResourceFlavor），不替换调度器本身，轻量、与原生调度器配合、多集群联邦友好，适合"只要排队和配额、不想重造调度器"的场景。

**一句话结论**：**要 Gang + AI 插件生态 → Volcano；要企业级层级配额 + 强抢占 → Yunikorn；只要排队准入、拥抱原生 → Kueue。**

**追问预判**：
- *「调度吞吐怎么量化对比？」*→ 用固定规模的模拟负载跑基准：单次调度循环 P99 延迟、每秒调度 Pod 数、大 Gang（128 卡）从提交到 Running 的时间。
- *「Volcano 相比原生的最大价值？」*→ 一句话：**把"能起"变成"能整组起"**——Gang + 队列配额 + 抢占，正好补上原生在 AI 批处理上的三个洞。

---

## Q8. 当节点上部分 GPU 出现 XID 硬件故障或降速（Slow Worker）时，调度系统如何快速感知并实现 Pod 驱逐、节点隔离（Cordon）与容错重调度？

**考察点**：故障自愈闭环（感知 → 隔离 → 重调度 → 恢复）的工程完备性。

**答题要点**：

1. **感知层**：
   - **DCGM / DCGM Exporter** 采集 Xid、ECC、温度、功耗、算力降频、NVLink 计数；
   - **dmesg / nvidia-smi -q** 抓驱动级报错；
   - **Node Problem Detector（NPD）** + GPU 插件把硬件异常转成 Node Condition/Event；
   - **慢节点检测（Slow Worker）**：监控每 step 耗时分布（正常训练极稳，长尾即异常）、NCCL 超时日志、节点 GPU 利用率"同步性"（正常训练各卡 SM 曲线同形，慢节点相位滞后）。
2. **隔离层**：
   - 对异常 GPU 标记 `Unhealthy`，Device Plugin 的 `ListAndWatch` **把该设备从 allocatable 移除**，从源头阻止新 Pod 落下；
   - 节点级 `cordon`（打 unschedulable）——全节点不可调度或仅剔除坏卡；
   - 分级处置：**一级**告警 + 不再接新任务；**二级**只接短任务、禁长跑；**三级**自动 drain 并报修。
3. **驱逐层**：对已落上坏卡的 Pod 做优雅驱逐（`eviction`，给 grace period），训练任务尽量等到 checkpoint 再迁；用 `preStop`/信号通知框架优雅退出。
4. **容错重调度层**：
   - Pod 重排时**避开被标记的坏节点/坏卡**（node taint + toleration/affinity）；
   - 训练任务按 checkpoint 恢复，优先恢复到原拓扑（保留 preferred 亲和），不满足时降级重分片（torchrun rendezvous 支持 world_size 变化）；
   - 预留 **hot spare** 节点加速恢复；重调度要**限流**防重试风暴。
5. **闭环**：故障事件留痕 → 累计 Xid 达阈值自动申请报修 → 修复后跑 benchmark 校验 → 重新纳入集群。

**追问预判**：
- *「Xid 里哪些可以继续用、哪些必须立刻隔离？」*→ 可恢复类（如 45 preemption、部分 43/68 视场景）标记观察；严重类（**79 掉卡、48/64 双 bit ECC、74 NVLink 错误**）立即隔离并报修。
- *「降速（热/功耗降频）算不算故障？」*→ 算"亚健康"：不立即驱逐，但标记并**优先在其上调度低优/短任务**，同时做趋势预测提前摘除。

---

# 第二部分：GPU 虚拟化、切分与多租户隔离

## Q9. 解释 NVIDIA MPS（Multi-Process Service）、vGPU（GRID）、MIG（Multi-Instance GPU）以及基于 CUDA Driver/Runtime 劫持（Hook）切分技术的本质区别与适用场景。

**考察点**：四种共享/切分技术的"隔离层级"认知——这是本套题的地基。

**答题要点**：

| 技术 | 本质机理 | 隔离维度 | 隔离强度 | 典型场景 |
|---|---|---|---|---|
| **MPS** | 多个进程的 kernel 通过一个**共享的 MPS Server** 提交到同一 CUDA Context，实现 SM 并发共享（可设 `active_thread_percentage` 限制每进程算力占比） | 算力（软，可配比）、**显存不隔离**、故障不隔离 | 中（进程级） | 同租户多进程推理聚合、要算力 QoS 又要弹性 |
| **vGPU / GRID** | Hypervisor + vGPU Manager 把物理卡**时分**切成若干 vGPU（驱动层时间片调度），配合 `vDWS / vApps / vCS` 授权 | 显存可硬限制（framebuffer 分区）、算力**时分不隔离** | 中（虚拟化层） | 虚拟桌面、图形/办公、早期云 GPU 租用 |
| **MIG** | 硬件把 GPU **物理切分**为多个 GI/CI 实例，SM / L2 / DRAM 均按份切，**独立故障域** | 显存 ✅ 物理隔离、算力 ✅ 固定切片、故障 ✅ 隔离 | **强（硬件级）** | 多租户生产推理、强隔离/合规、显存需求稳定 |
| **CUDA Hook（API 拦截 / 驱动劫持）** | 用户态 `LD_PRELOAD` 拦截 CUDA Driver API（如 `cuMemAlloc`）或内核驱动拦截，做显存记账/限额、算力限流（代表：HAMi、腾讯 qGPU、阿里 cGPU、cGPU 系列） | 显存（软记账/硬限）、算力（软限流）、故障不隔离 | 弱～中（软件层） | 兼容性要求广（任意卡）、细粒度显存配额、成本敏感的多租户 |

**一句话记忆**：**MIG 切硬件（最硬但最贵最不灵活），vGPU 切虚拟化层，MPS 共享上下文（要弹性），Hook 切软件（最灵活最弱）**。

**追问预判**：
- *「为什么数据中心逐渐少用 vGPU？」*→ 授权成本高、时分隔离弱、MIG 出现后在"生产多租户"这一生态位上被替代；vGPU 仍活跃在桌面/图形虚拟化。
- *「Hook 方案最大的坑？」*→ 应用可以不经过被拦截的 API（静态链接、`dlopen` 绕过、直接走 ioctl），导致"拦不干净"，所以要与驱动层/容器 runtime 结合。

---

## Q10. MIG（Multi-Instance GPU）在物理切分时如何保证故障隔离与 QoS？调度层如何感知并管理动态创建的 MIG 实例？

**考察点**：MIG 的隔离原理 + K8s 侧落地细节。

**答题要点**：

1. **故障隔离原理**：MIG 把 SM、L2 cache slice、DRAM 通道、DMA 引擎等按实例物理划分，各 GI（GPU Instance）/CI（Compute Instance）**拥有独立的硬件资源与独立的错误域**——一个实例上的 ECC 错误或 Xid **不会传播**到其他实例，这是 MIG 独有的"故障隔离"能力（MPS / 时间片都做不到）。
2. **QoS 保证**：算力与显存带宽被固定切分，实例之间不抢 SM/L2/带宽（差异主要来自共享的少量组件如部分 copy engine、L2 之外的 crossbar，存在轻微干扰但可接受）；因此延迟抖动小，适合有 SLO 的生产推理。
3. **常见 profile（A100 / H100）**：`1g.5gb / 2g.10gb / 3g.20gb / 4g.40gb / 7g.80gb`（H100 94GB 为 `1g.10gb` 等）；切分必须先选 profile 再枚举可创建实例。
4. **调度层感知与管理**：
   - **上报**：Device Plugin 以 MIG 策略工作——`single`（把每个 MIG 实例作为独立资源 `nvidia.com/mig-1g.5gb` 上报）或 `mixed`（同时上报 `nvidia.com/gpu` 与 MIG 资源）；
   - **调度**：MIG 实例作为 extended resources，Pod 声明对应 profile 数量，调度器按"最小满足档位"匹配（避免大切片跑小任务）；
   - **动态创建**：`nvidia-mig-manager` / 自研 Agent 按策略创建实例，创建完成后触发 Device Plugin 重新上报（allocatable 变化）；**重切分需要排空该卡**，必须放在低谷/变更窗口，并做迁移预案；
   - **生命周期**：节点重启后实例不保留，需自动重建切分方案（声明式 profile 配置 + reconcile）。
5. **代价边界**：MIG 切片间**无 NVLink**、重配置慢 → **不适合需要卡间高速互联的分布式训练**；更适合推理与单卡多租。

**追问预判**：
- *「MIG 实例之间的算力是否完全无干扰？」*→ 计算与显存带宽基本隔离；仍共享少量组件（如部分引擎/PCIe 上行），极端情况下有轻微干扰，但远小于 MPS/时间片。
- *「怎么决定 MIG 池和整卡池的比例？」*→ 按历史任务粒度分布动态调整，并做**分时重切**（白天整卡跑大任务、夜间重切成小片喂小任务）。

---

## Q11. 基于用户态 LD_PRELOAD 或内核驱动拦截实现的 GPU 显存限制方案，如何精确捕获并控制 cuMemAlloc / cudaMalloc 的显存申请与释放？

**考察点**：软件层显存管控的实现细节，是否真的动手做过。

**答题要点**：

1. **拦截哪一层、哪几个 API**：
   - CUDA 有两套 API：**Runtime**（`cudaMalloc / cudaFree / cudaMallocAsync`）与 **Driver**（`cuMemAlloc / cuMemFree / cuMemAlloc_v2`），以及更底层的**虚拟内存管理 VMM API**（`cuMemCreate / cuMemMap / cuMemAddressReserve`，CUDA 10.2+）。要全拦，不能只拦 `cudaMalloc`；
   - 主流做法：拦截 **Driver API**（Runtime 最终也落到 Driver），用 `dlsym(RTLD_NEXT, ...)` 拿到真实函数指针转发。
2. **LD_PRELOAD 的实现路径**：编译一个 `libvgpu.so`，实现同名符号（`cuMemAlloc` 等），在其中调用真实实现成功后**记账**（进程显存用量累加），失败或超限则返回 `CUDA_ERROR_OUT_OF_MEMORY`；容器启动时通过 `LD_PRELOAD` 注入（Device Plugin 在 `Allocate` 时设置环境变量 + 挂载 so 文件）。
3. **精确性难点与对策**：
   - **分配器缓冲（caching allocator）**：PyTorch 的显存分配器会"先申请一大块再自己切"，导致"申请量 ≫ 实际使用量"；对策是对齐"按申请上限限制"的策略，或允许"软超卖 + 运行时驱逐"，或读取框架实际使用量校准；
   - **绕过拦截**：静态链接、`dlopen` 自行加载、直接 `ioctl` 走驱动 → 需要**内核驱动层**或容器 runtime（`nvidia-container-toolkit` 的 prestart hook）兜底；
   - **多 API 混用**：`cudaMallocAsync`、cuBLAS/推理引擎内部池化 → 必须覆盖全部内存分配入口。
4. **释放与回收**：拦截 `Free` 类 API 做减账；进程退出时清账；跨进程共享同一卡时要集中记账（节点 Agent / 共享内存）。
5. **限流语义**：显存超限时应**快速报错（OOM）**而非静默等待，避免调用方 hang 死；同时与调度侧的配额保持一致，防止"调度给了 8G、运行时只放 4G"。

**追问预判**：
- *「为什么必须拦 Driver API 而不能只拦 Runtime？」*→ Runtime 是高层封装且可能被静态链接；Driver API 是所有路径的必经之处，拦截面更可靠。
- *「只做显存限制不做算力限制够吗？」*→ 不够，会出现"显存够但 SM 被邻居打满"的饥饿，需要配合 MPS 配比或时间片配额（见 Q12）。

---

## Q12. 在算力隔离（Compute Isolation）层面，如何利用时间片（Time-Slicing）或 Token 机制防止高优任务被低优共乘任务饿死？

> ⭐ **本套题库的高频重点题**，图内自带完整示范解析，见下方第三节「重点题精讲」。

---

## Q13. GPU 显存超卖（Overcommit）在真实业务中是否可行？通过 UVM（Unified Virtual Memory）或 Host-GPU 内存置换（Swapping）实现超卖的性能损耗有多大？

**考察点**：对超卖收益/风险/性能代价的量化判断，避免"为了利用率指标乱超卖"。

**答题要点**：

1. **可行性的前提**：超卖的本质是"赌不是所有任务同时用满显存"。**算力可以超卖，显存超卖要非常谨慎**——算力超卖最坏是变慢，显存超卖最坏是 **OOM 崩溃**（且可能拖垮同卡其他任务）。
2. **UVM（Unified Memory）机理与损耗**：
   - `cudaMallocManaged` 分配的是统一地址空间，物理页按需在 host/device 之间迁移（page migration）；
   - **顺序访问 + 预取（prefetch）**：损耗可接受（带宽受限于 PCIe/NVLink 链路）；
   - **随机访问（pointer chasing）**：会引发大量 **page fault + 页迁移**，延迟可放大到**数百倍**，吞吐塌陷——这是 UVM 最主要的坑。
   - Grace-Hopper（GH200）的 **NVLink-C2C（~900GB/s）** 让 host 内存近似"大显存"，性能损耗远小于 PCIe 时代，使"显存扩展"更可行，但普通 PCIe 卡（~32/64GB/s）迁移代价依然显著。
3. **Host-GPU Swapping（显存换出）**：
   - 推理场景可以做**KV cache 换出/换入**（类似 vLLM 的 CPU offload），但会显著抬升 **TTFT/尾延迟**；
   - 显存热点 swap 会造成 PCIe 带宽瓶颈（尤其多卡共享同一 PCIe switch 时）。
4. **量化口径（面试敢给数）**：PCIe 4.0 x16 约 32GB/s 有效，对比 HBM3 ~3TB/s 是**两个数量级**差距；因此"显存换出"只能作为**冷数据的兜底**，不能作为热路径；UVM 超卖典型场景下有效吞吐可能掉到 **20%~50%**（视访问局部性）。
5. **安全超卖的工程做法**：
   - **准入防线**：超过节点超卖阈值拒绝新任务；高优/在线任务禁止落在超卖卡；
   - **硬限制防线**：显存用 vGPU 硬上限或 MIG 物理隔离，**只超"软"的部分**；
   - **运行时防线**：Agent 监控显存水位，接近 OOM 时按优先级驱逐低优任务；
   - **分级策略**：低优离线任务可超卖 1.2–1.5×，生产推理**基本不超卖显存**、只轻超卖算力；
   - **兜底**：被驱逐任务自动重排（带欠条优先恢复）。

**追问预判**：
- *「为什么不用 UVM 直接把显存当无限用？」*→ 随机访问下 page fault 风暴会让性能不可接受；只在"数据有强局部性 + 可预取"或 NVLink-C2C 平台才划算。
- *「超卖导致 SLA 违约算谁的？」*→ 超卖策略本身要带 **SLA 承诺分级**，低优任务超卖即接受 best-effort，契约先行。

---

## Q14. 多个 Pod 共享一张 GPU 时，如何防止某个 Pod 发生 CUDA Error 导致整卡 Context 崩溃而波及其他容器？

**考察点**：共享场景下的故障域认知 + 多租户隔离的底线设计。

**答题要点**：

1. **为什么会互相波及**：
   - 非 MIG 共享下，多个进程共用同一个 **GPU Context / 驱动实例**；驱动级的致命错误（**Xid 79 掉卡、ECC 不可纠正错误、GPU 挂死、驱动崩溃**）会让**整块卡不可用**，所有进程一起挂；
   - 一个进程疯狂占用显存导致 **OOM**，可能触发全卡 context 重置；
   - 某些特权操作（`nvidia-smi --gpu-reset`、驱动 ioctl）会被一个容器影响到整卡。
2. **分层防护**：
   - **最彻底：MIG 物理隔离** —— 独立故障域，一个切片的 Xid 不影响其他切片，是跨租户共享的**首选**（有合规/稳定性要求时只认 MIG）；
   - **显存防护**：vGPU/驱动硬上限（防超用 OOM）；在线任务预留显存 watermark，共享任务只能用 watermark 之外的部分；
   - **算力防护**：MPS 配比限制，防打满饿死；
   - **行为限制**：cgroup 限 CPU/IO；**禁止容器内执行特权驱动操作**（seccomp/权限收敛，拦 `nvidia-smi --gpu-reset`）；
   - **进程级自愈**：节点 Agent 监控 Xid / 挂死进程，自动隔离并重启**单个异常进程**，标记坏卡。
3. **兜底与追责**：异常租户熔断（暂停其任务）、事件留痕、必要时驱逐其全部 Pod 并回收资源。
4. **决策原则**：**隔离强度决定共享范围**——可信租户之间可软共享（MPS/时间片），**跨租户/有合规要求必须 MIG**；共享方案要明示"不保证故障隔离"。

**追问预判**：
- *「MPS 下能隔离 CUDA Error 吗？」*→ 不能。MPS 隔离算力配额，但共享驱动上下文，致命错误仍可能殃及全卡。
- *「一个 Pod OOM 会不会杀掉别人的进程？」*→ 会（非 MIG 场景）。OOM 可能触发 context 重建/驱动复位，所以显存硬隔离（MIG/vGPU 上限）是共享的前提。

---

# 第三节：重点题精讲 —— Q12 算力隔离防饿死（图内完整示范解析整理）

## 3.1 面试官考察角度

- **核心考察能力**：对 **GPU 任务调度策略**的理解，特别是共享环境下的**优先级保证**与**抢占机制**。
- **隐藏考察点**：面试官真正想听的是你对「**长尾延迟**」和「**吞吐量 vs 响应速度**」权衡的思考。
- **评判标准**：
  - 是否提到 **NVIDIA 原生 Time-Slicing 配置**；
  - 是否能讲出**自定义 Token 桶算法**；
  - 是否理解通过**控制并发流（Stream）**实现优先级。
- **常见陷阱**：误以为 **K8s 的 CPU 优先级（PriorityClass）能直接作用于 GPU 算力隔离**。实际上 **GPU 是非抢占式的**，除非驱动层支持——这是最容易被刷掉的认知错误。

## 3.2 回答思路框架

- **现状痛点**：GPU 任务的**非抢占性（Run-to-completion）**——一个 kernel 一旦开始跑，必须跑完，无法被抢占。
- **方案一**：原生 **Time-Slicing**（简单，但**无优先级**）。
- **方案二**：基于 **Token 的网关控制**（应用层 / 驱动劫持层）。
- **方案三**：**MPS 优先级设定**（最推荐）。
- **总结**：如何通过「**配额 + 抢占**」实现防饿死。

## 3.3 优秀回答示例（完整版 · 图内原文整理）

> **开场定调**：在 GPU 算力隔离中，最难的点在于 **GPU 内核任务通常是"不可抢占"的**。为了防止高优任务被饿死，我们通常从**三个层面**进行控制：

**第一，利用 MPS 的执行资源限制（Execution Resources）。**
在 MPS 环境下，我们可以为不同的 Client 进程设置 `active_thread_percentage`。对于**高优任务不设限**；对于**低优任务限制其仅能使用 20% 的 SM 资源**。这样即使低优任务满载，高优任务依然有**预留的硬件通道可以立即执行**。

**第二，引入基于 Token 桶的调度网关。**
在劫持层（Hook），我们为每个任务维护 Token。高优任务拥有"**无限/高额**"Token，而低优任务在提交 CUDA Kernel 前**需要获取 Token**。如果高优任务到来，我们会**动态收紧低优任务的 Token 发放速率**，甚至通过 `cudaStreamSynchronize` **强制插入空档**，从而腾出算力。

**第三，K8s 层的协同。**
虽然 K8s 不能直接控制 GPU 时间片，但我们可以通过**自定义调度器**，在感知到高优任务待调度时，**主动驱逐（Evict）占用 GPU 的低优 Pod**，或者利用 NVIDIA 最新驱动支持的 **Time-Slicing 权重配置**，将高优 Pod 映射到**拥有更多时间片配额的虚拟 GPU 实例**上。

> **收尾**：通过这种「**硬件预留 + 软件流控**」的组合拳，我们可以保证高优任务的 **P99 延迟在共乘环境下依然保持稳定**。

## 3.4 构建优秀回答的方法论（图内原文整理）

- **底层逻辑**：区分「**并发控制**」和「**抢占控制**」。**GPU 很难做真抢占，所以重点应放在"限制低优任务的并发度"上**。
- **构建方法**：给出**多维度**的方案（**MPS、Hook、K8s**），并解释每种方案**如何解决"饿死"问题**，体现方案的完备性。

## 3.5 负面回答示例及分析（图内原文整理）

> **不好的回答**："我们可以给高优任务设置更高的 K8s 优先级。当高优任务来的时候，K8s 会把低优任务杀掉，这样高优任务就有卡用了。或者用时间片轮转，大家平分算力，这样谁也不会饿死。"

**为什么不好**：

1. **杀掉任务（Eviction）代价太大**，不适合所有场景（训练任务 checkpoint 恢复成本极高）；
2. **平分算力（Time-Slicing）恰恰无法保证高优任务的优先权**——高优任务需要的是「**更多**」或「**更及时**」的算力，而不是「**公平**」；
3. **回答缺乏对 MPS 权重等关键技术的认知**——只停留在 K8s 层面，没触及真正的算力隔离手段。

## 3.6 本题加分延伸（建议补充）

- **量化指标**：高优任务 **P99 延迟**、低优任务**吞吐降幅**、Token 发放速率与实际 SM 占用率的相关性；
- **兜底策略**：当高优突发导致低优长期无 Token 时，要有**饥饿保护**（低优保底 Token），否则违反"防饿死"初衷——即**防饿死 ≠ 让低优饿死**；
- **与 Q9/Q22 联动**：MPS 只能管算力、不隔离显存与故障，因此完整方案通常是 **MIG（显存+故障隔离）+ MPS（算力配比）+ Hook（细粒度流控）** 组合。

---

# 第四节：扩展题库（42 题）

> 按图内 14 题的主题逐一**扩展**，每题给出「考点 + 一句话答题要点」，可先自答再对要点。用于把"会答这一题"升级为"这一片都会答"。

## 扩展 A 组：K8s 调度器底层工程（对应 Q1/Q2）

- **EA1. K8s 调度器的 ActiveQ / BackoffQ / UnschedulableQ 三个队列分别解决什么问题？调度失败的 Pod 何时被重试？**
  > 考点：调度语义吃透度。要点：ActiveQ 是堆排的待调度队列；BackoffQ 做指数退避（冲突/失败后延迟重试，防止热循环）；UnschedulableQ 暂存"约束不满足"的 Pod，靠**集群事件驱动**唤醒（新增节点、Pod 删除等）并配定时兜底。核心设计目标是**避免无意义的重试风暴**。
- **EA2. Filter 与 Score 的执行模型是什么？`percentageOfNodesToScore` 的作用？**
  > 要点：Filter 串行过滤（不可行节点淘汰），Score 对可行节点并行打分并归一化加权求和；`percentageOfNodesToScore` 让大集群只采样部分节点即停止，平衡延迟与选型质量（节点越多采样比例越低，下限约 5%）。
- **EA3. 一个 Pod 从 Pending 到 Running，要经过哪些参与方？Device Plugin 的 Allocate 在哪一步被调用？**
  > 要点：调度（选节点）→ kubelet 准入 → Device Manager 调 Device Plugin 的 `Allocate`（准备设备/注入）→ 容器运行时创建容器；**调度器只是"选节点"，真正绑设备在 kubelet 侧**。
- **EA4. 为什么说"调度与设备绑定之间存在两阶段窗口"？这个窗口会带来什么风险？**
  > 要点：调度器决定"节点有卡"→ 数毫秒/秒后才由 kubelet 分配具体 UUID；窗口内节点状态可能变化 → 需要 **Assume/Reserve 占位** 与失败回滚，GPU UUID 需自己做 CAS 保护。

## 扩展 B 组：GPU 资源表达与细粒度切分（对应 Q2/Q4）

- **EB1. `nvidia.com/gpu: 2`（整数计数）与 `nvidia.com/gpumem: 8192`（显存维度）在调度器眼里有什么本质不同？**
  > 要点：前者是**可累加计数**，后者是**二维约束（显存+算力）**；多维度资源可做更细的 binpack，也带来"多维碎片"问题。
- **EB2. 半张卡、1/4 卡在 K8s 里怎么表达？各自的取舍？**
  > 要点：把卡切成 N 个逻辑设备（粒度固定、实现简单）vs 自定义 `gpumem/gpucores`（灵活、需 Webhook+Agent 支持）；DRA 是未来方向。
- **EB3. 节点重启后，MIG/vGPU 的切分状态如何恢复？调度器怎么对齐？**
  > 要点：切分是**非持久状态**——需声明式 profile 配置 + Agent reconcile 重建；Device Plugin 重新上报 allocatable；未重建完成前该节点不应被调度。
- **EB4. 细粒度切分后，如何防止"显存超发"？准入、运行时、调度三层各做什么？**
  > 要点：Webhook 准入校验节点剩余；运行时硬上限（vGPU/MIG）；调度层保守估计 + 水位预留；三层缺一不可。

## 扩展 C 组：拓扑感知与并行策略（对应 Q3）

- **EC1. `nvidia-smi topo -m` 里的 NV#、PIX、PHB、SYS 分别代表什么？放置时如何排序？**
  > 要点：NV# 最优（NVLink），PIX 同 PCIe switch，PHB 同 host bridge，SYS 跨 NUMA（最差）；多卡任务优先 NV#>PIX>PHB>SYS。
- **EC2. 8 卡 NVSwitch 全互联 vs 双 NUMA 各 4 卡，对 TP/DP 放置分别意味着什么？**
  > 要点：NVSwitch 全互联对任意并行策略都友好；双 NUMA 下 TP 组要锁在**同一 NUMA 的 4 卡**内，跨 NUMA 的 SYS 链路会成为 TP 瓶颈。
- **EC3. 跨机 Rail-optimized 布线是什么？对调度放置有什么要求？**
  > 要点：GPU i 固定对网卡 i，同一 rail 内通信等价；调度需把 **DP 通信组尽量放在同一 rail/leaf**，避免跨 spine 流量翻倍。
- **EC4. 拓扑约束与 binpack 冲突时怎么取舍？**
  > 要点：拓扑是**性能硬需求**、binpack 是**利用率偏好**；生产上"训练按拓扑严格、离线按 binpack 紧凑"，靠队列/profile 分池解决。

## 扩展 D 组：Gang 调度与死锁（对应 Q5）

- **ED1. Volcano 的 PodGroup 与 coscheduling 插件的 Permit 机制，各自如何实现"全组原子"？**
  > 要点：PodGroup 声明 minMember/minResources，插件在未达成员数前只 Reserve 不放行，达标后统一 Permit→Bind；超时则整组回滚。
- **ED2. Gang 死锁检测与超时回滚，哪个更可靠？为什么？**
  > 要点：超时回滚**简单可靠**（工程主流）；等待图检测更优但实现复杂、维护成本高，通常作为兜底增强。
- **ED3. 资源不足时，应该"整组等待"还是"降级小规模启动"？**
  > 要点：看任务语义——严格 Gang（同步训练）必须等；弹性训练允许降到 min replicas 启动再扩（torchrun）。调度器要能识别弹性区间。
- **ED4. 抢占与 Gang 结合时，如何保证 victim 选择的整组一致性？**
  > 要点：要么整组抢占、要么不抢，避免"抢了半个 PodGroup"造成新的部分占用；且 victim 组内的 checkpoint 成本要一并评估。

## 扩展 E 组：多租户层级队列与配额（对应 Q6）

- **EE1. 层级队列的 min/max 如何随父子结构聚合与继承？**
  > 要点：父 min ≈ 子 min 之和（硬保底），父 max 由集群总量与兄弟共享决定；未单独设配额的子队列继承父策略。
- **EE2. 弹性借调如何避免不同父队列间的环形等待？**
  > 要点：只允许"子借父、父内回收"，禁止跨父子树互相借调；配合 credit 记账与抢占限流。
- **EE3. 分时配额（time-based quota）怎么实现？典型场景是什么？**
  > 要点：按时间段加载不同配额模板（白天保在线、夜间放训练）；配 Cron 或调度器配置热加载；场景即"在离线混部"。
- **EE4. 配额与实际使用长期偏差大，怎么校准？**
  > 要点：按使用率考核，长期闲置/低效的配额回收；对超用做内部计费；季度复盘调模板。

## 扩展 F 组：调度器选型与吞吐（对应 Q7）

- **EF1. Volcano 的组件（vc-scheduler / vc-controller-manager / vc-webhook-manager / vc-agent）各做什么？**
  > 要点：scheduler 出调度与插件；controller-manager 管队列/PodGroup 生命周期；webhook 做准入与默认值；agent 在节点侧做设备/网络增强。
- **EF2. Yunikorn 的"应用级调度"是什么意思？为何对 AI 作业友好？**
  > 要点：把一组 Pod 视为一个"应用"整体调度（天然 Gang），队列配额以应用为单位，避免逐 Pod 竞争；源自 YARN 的多租户基因。
- **EF3. Kueue 与原生调度器的职责边界？它解决"排队"还是"调度"？**
  > 要点：Kueue 只做**配额准入与排队**（ClusterQueue/LocalQueue/ResourceFlavor），Pod 何时被创建由它决定、放哪台机器仍归 kube-scheduler。
- **EF4. 如何给"调度吞吐"做基准测试？看哪些指标？**
  > 要点：模拟大规模负载，测单次调度循环 P99 延迟、每秒调度 Pod 数、大 Gang 端到端启动时延、抢占/重调度抖动次数。

## 扩展 G 组：故障感知与自愈（对应 Q8）

- **EG1. Xid 常见码（13/31/43/45/48/63/64/68/74/79/92/94/95）各代表什么？哪些必须立刻隔离？**
  > 要点：13 图形引擎异常、31 MMU 缺页（常应用侧）、43 GPU 停止处理、45 抢占（良性）、48/64 双 bit ECC、63 ECC 页退休、68 视频引擎、74 NVLink 错误、79 掉卡（**必须立刻隔离**）、92/94/95 被隔离的 ECC 错误（可观察）。
- **EG2. GPU 掉卡（Xid 79）后，整条处置链路是什么？**
  > 要点：DCGM/NPD 感知 → Device Plugin 摘除设备 → cordon 节点 → 驱逐 Pod → 重调度避开 → 报修 → 修复后 benchmark 校验回归。
- **EG3. Node Problem Detector 与 DCGM Exporter 在故障感知里各是什么角色？**
  > 要点：DCGM 提供**指标度量**（Xid/ECC/温度/降频），NPD 把问题**转成 K8s Node Condition/Event** 触发隔离动作；NVIDIA GPU Operator 把两者与 device plugin 打包运维。
- **EG4. 如何设计训练的"故障自愈"闭环，减少人工介入？**
  > 要点：checkpoint 自动化 + 断点续训 + hot spare 节点 + 重调度限流 + 亚健康预测（趋势上升即提前摘除）。

## 扩展 H 组：四种切分技术的本质（对应 Q9）

- **EH1. 从"隔离强度"给 MIG / vGPU / MPS / Hook 排序，并说明各自的适用边界。**
  > 要点：MIG（硬件）> vGPU（虚拟化层）> MPS（进程级）> Hook（软件层，最弱）；选型按"是否跨租户/是否要故障隔离/是否要弹性"三问定。
- **EH2. 为什么 vGPU（GRID）在数据中心多租户推理场景逐渐被 MIG 取代？**
  > 要点：授权成本、时分隔离弱、MIG 在"强隔离+可调度"上更契合；vGPU 生态仍在桌面/图形虚拟化。
- **EH3. Hook 方案的三种拦截点（API 拦截 / 驱动劫持 / 容器 runtime hook）各自优劣？**
  > 要点：API 拦截（LD_PRELOAD）易实现但可被绕过；驱动劫持更彻底但风险高；runtime hook 只做注入与配置，需与前两者配合。
- **EH4. 用一句话给"该选哪种切分方式"画个决策树。**
  > 要点：跨租户/要故障隔离→MIG；要算力弹性 QoS→MPS；桌面/图形→vGPU；要细粒度显存配额且兼容任意卡→Hook。

## 扩展 I 组：MIG 深挖（对应 Q10）

- **EI1. MIG 的 GI 与 CI 分别是什么？一个 GPU 实例能包含几个计算实例？**
  > 要点：GI（GPU Instance）是按 SM/内存切片划分的实例，CI（Compute Instance）是 GI 内的计算引擎单元（一般 1 GI = 1 CI，部分 profile 支持多 CI）。
- **EI2. MIG 实例之间存在哪些"共享资源"，可能带来什么干扰？**
  > 要点：SM/L2/DRAM 已物理切分，仍共享少量组件（部分引擎、PCIe 上行、主机接口）；极端负载下存在轻微干扰，但远低于 MPS/时间片。
- **EI3. 为什么 MIG 重切分必须排空该卡？代价如何缓解？**
  > 要点：切分是卡级硬件重配置，不能带负载进行；缓解方式为分时重切（低谷窗口）+ 声明式 profile + 迁移预案。
- **EI4. K8s 里 MIG 的 single / mixed 两种策略有什么区别？**
  > 要点：single 只暴露 MIG 实例资源；mixed 同时暴露整卡与 MIG 资源，便于整卡大任务与切片小任务共存于同集群。

## 扩展 J 组：显存拦截与控制（对应 Q11）

- **EJ1. `cudaMalloc` / `cuMemAlloc` / `cuMemCreate` 有什么区别？为什么要拦截多个 API？**
  > 要点：Runtime vs Driver vs VMM 三层；VMM API（`cuMemCreate/cuMemMap`）绕过传统分配，只拦 `cudaMalloc` 会漏记。
- **EJ2. 框架的 caching allocator 导致"申请量 ≠ 使用量"，显存限制怎么穿透？**
  > 要点：按"申请上限"限制（保守）或读框架真实使用量校准；配合软超卖 + 运行时驱逐；口径必须与调度侧配额一致。
- **EJ3. LD_PRELOAD 的局限性（静态链接、dlopen、ioctl）如何兜底？**
  > 要点：内核驱动拦截 + `nvidia-container-toolkit` prestart hook 注入；多手段叠加，单靠 LD_PRELOAD 不可靠。
- **EJ4. 显存超限时应报错还是等待？OOM 语义如何与业务对齐？**
  > 要点：**快速返回 OOM 错误**（避免 hang），并给出清晰的错误与配额提示；与调度侧配额保持同一口径。

## 扩展 K 组：算力隔离与防饿死（对应 Q12）

- **EK1. MPS 的 `active_thread_percentage` 粒度是多少？对"算力隔离"到底约束了什么？**
  > 要点：限制进程可用的 SM 线程比例（如 20%），约束的是**并发线程资源**而非时间片；对 GPU 内核"不可抢占"问题的解法是"限制并发度"。
- **EK2. Time-Slicing 的时间片粒度如何配置？对尾延迟的影响是什么？**
  > 要点：由设备插件（如 `timeSlicing.replicas`）配置共享份数，驱动层轮转；一个长 kernel 会把其他任务的延迟放大一个时间片——因此**不适合延迟敏感的生产推理**。
- **EK3. Token 桶的参数（速率、桶容量）怎么标定？如何防止"饥饿保护"被高优突发打破？**
  > 要点：速率按高优/低优配额比与实测 SM 占用相关性标定；低优设**保底 Token**，避免在防饿死的同时把低优饿死。
- **EK4. "长尾延迟"和"吞吐 vs 响应速度"的权衡如何量化？**
  > 要点：看高优 P99/P999 与低优吞吐的**帕累托前沿**；调 MPS 配比/Token 速率，找到既保 P99 又不牺牲过多吞吐的拐点。

## 扩展 L 组：显存超卖（对应 Q13）

- **EL1. UVM（cudaMallocManaged）的实现原理是什么？什么访问模式下性能会崩塌？**
  > 要点：统一地址空间 + 页按需迁移；**随机访问**引发 page fault 风暴，延迟放大数百倍；顺序访问 + prefetch 才可接受。
- **EL2. 显存 swap 到 host 的带宽瓶颈在哪？PCIe 与 NVLink-C2C 差距多大？**
  > 要点：PCIe 4.0 x16 ~32GB/s vs HBM ~3TB/s（差两个数量级）vs NVLink-C2C ~900GB/s；决定"显存扩展"是否可行的硬件底座。
- **EL3. 哪些场景可以安全超卖显存？哪些绝对不能？**
  > 要点：低优离线/预处理可超卖 1.2–1.5×；生产推理、延迟敏感任务**不超卖显存**（只轻超卖算力）。
- **EL4. 超卖后接近 OOM 时，回收/驱逐策略怎么设计？**
  > 要点：水位触发 → 按优先级驱逐低优 → 带欠条优先重排 → 事件上报并自动回退超卖策略。

## 扩展 M 组：多进程共享的故障隔离（对应 Q14）

- **EM1. 为什么非 MIG 共享下，一个进程的 CUDA Error 会波及整卡？**
  > 要点：共享同一驱动实例/GPU Context；致命错误（Xid 79、不可纠正 ECC、挂死）导致**整卡复位**，所有进程陪葬。
- **EM2. 为什么 MIG 能隔离故障，而 MPS/时间片不能？**
  > 要点：MIG 是**物理独立实例 + 独立错误域**；MPS/时间片共享驱动上下文，错误边界是整个 GPU。
- **EM3. 共享场景下如何做"进程级自愈"？**
  > 要点：Agent 监控 Xid/挂死 → 隔离并重启单个异常进程 → 标记坏卡 → 通知受害者任务重试。
- **EM4. 如何在容器内限制 `nvidia-smi --gpu-reset` 等特权操作？**
  > 要点：权限收敛 + seccomp/AppArmor + 不给设备写权限 + privileged 容器白名单；从"谁能碰驱动"层面做多租户防护。

---

# 第五节：整体答题方法论（从本套题提炼）

1. **先分清"控制"的两种范式**：GPU 内核**不可抢占**，所以绝大多数优先级问题的正解是「**限制低优任务的并发度**」，而非「抢占/杀任务」——这是本套题最核心的底层逻辑。
2. **多维度作答**：凡是"如何隔离/如何保优先级"，都要给 **MPS + Hook + K8s** 或 **MIG + MPS + Hook** 的组合拳，单方案一定不完备。
3. **主动讲代价**：MIG 重配置慢、无 NVLink；MPS 不隔离显存与故障；时间片放大尾延迟；超卖换来利用率但换来 OOM 风险——**每个方案都有代价**，说出来才可信。
4. **区分"调度层"与"运行时层"**：调度器只决定"资源给谁"，真正的隔离动作在 kubelet/Device Plugin/驱动层；能否讲清这条"两层协同 + 一次对齐"的链路，是区分"背概念"和"做过"的关键。
5. **敢给数字**：PCIe vs HBM 的两个数量级、UVM 随机访问放大数百倍、超卖 1.2–1.5×、MPS 20% SM、P99 延迟——量化口径是专家感的来源。

---

# 第六节：与既有面试资料的衔接建议

本套（14 题 · 偏 Infra 底层）与工作区既有资料构成互补，建议合起来用：

| 资料 | 侧重 | 本套如何补强 |
|---|---|---|
| 《GPU调度面试题库-27题全解析.md》 | 调度策略 / 资源运营 / 公平性 | 本套补 **底层机制**：Q9–Q14 是 27 题 Q21–Q27 的"向下钻取"（27 题讲"选 MIG 还是 MPS"，本套讲"MIG 为什么能隔离故障 / MPS 怎么配比 / Hook 怎么拦 API"） |
| 《GPU调度面试-覆盖度评估与补强题库.md》 | K8s 调度器底层 A 组 / 通信 B 组 | 本套 Q1–Q3 **直接命中原 A 组缺口**（Framework 扩展点、拓扑感知），可与 A1–A6 合并复习 |
| 《GPU调度面试-TOP10逐字口述稿.md》 | 口述稿 | 建议为本套 **Q12 + Q1 + Q9** 各补一篇逐字稿（Q12 图内已给示范，改动即可用） |

**新增待办方向**：本套补齐了"虚拟化/切分/隔离"这一维度后，仍可继续补的是——**DRA（Dynamic Resource Allocation）实战**、**HAMi / qGPU 源码级细节**、**NVIDIA GPU Operator / MIG Manager 运维实操**、**GB200 / NVL72 SuperPod 的调度新约束**。

---

> 整理自用户上传的 9 张题库截图；扩展题目为基于主题的系统性补强，非图内原有内容。
