# GPU 调度岗位面试：27 题库覆盖度评估 + 补强题库

> 配套文档：《GPU调度面试题库-27题全解析.md》

---

## 一、整体结论

**这套 27 题的覆盖定位**：它是非常标准、质量很高的「**GPU 资源共享与调度策略**」专项题库，深度足够，尤其难的是把"SLA/公平性/利用率/推动业务"这条链路打通了。

**但它在面试整体要求里的真实占比，取决于岗位 JD 侧重点**：

| 岗位侧重点 | 27 题覆盖率 | 说明 |
|---|---|---|
| GPU 调度策略 / 资源共享（JD 职责聚焦「资源抽象、配额、优先级与抢占机制、GPU 共享、构建 GPU 集群调度系统」这类） | **70–80%** | 题目高度贴合这类 JD，很可能直接来自实际业务场景 |
| GPU 集群 / AI Infra 平台研发（要有实际大规模产品经验） | **40–50%** | 缺分布式训练通信、故障自愈、推理服务化、K8s 底层扩展、多集群弹性 |
| 资深/专家岗（要求：系统设计 + 技术判断力 + 业务影响力） | **50%** | 缺白板架构设计、技术演进方向判断、STAR 深挖与产品思维 |

**一句话**：这套题能让你"答得漂亮"，但不足以支撑"我和真做过的人一样"。补齐下面的缺口才能覆盖专家岗全链路。

---

## 二、缺口雷达（按模块）

| # | 能力模块 | 27 题现状 | 缺口等级 | 必补内容 |
|---|---|---|---|---|
| 1 | 资源抽象与调度算法 | ✅ 覆盖很好（Q5/6/7/11-13） | 低 | 白板代码能力（见 F 组） |
| 2 | 队列/配额/公平/SLA | ✅ 覆盖很好（Q8/9/10） | 低 | 与 Yunikorn/Volcano/Kueue 具体实现对照 |
| 3 | GPU 虚拟化与共享 | ✅ 覆盖很好（Q21-27） | 低 | DRA（Dynamic Resource Allocation）新特性、HAMi 源码级细节 |
| 4 | 利用率优化与运营 | ✅ 覆盖很好（Q14-20） | 低 | FinOps 成本模型、单位卡时成本核算 |
| 5 | **K8s 调度器底层工程** | ⚠️ 只有 Q2 一笔带过 | **高** | A 组 6 题 |
| 6 | **分布式训练与通信拓扑** | ❌ 完全缺失 | **高** | B 组 6 题 |
| 7 | **推理服务侧调度** | ❌ 几乎缺失（仅 Q17/Q23 提及） | **高** | C 组 5 题 |
| 8 | **稳定性/故障/运维** | ⚠️ 分散提及，无系统性问题 | **中高** | D 组 4 题 |
| 9 | **成本/产品化/技术选型** | ⚠️ Q16/Q18 部分触及 | **中高** | E 组 4 题 |
| 10 | **算法与白板编码** | ❌ 完全缺失 | **高** | F 组 4 题 |
| 11 | **STAR / 行为面深挖** | ❌ 缺失 | **中** | G 组 3 题 |

---

## 三、补强题库（32 题）

> 每题给「考察点 + 答题要点」，可先自己答再对要点。

### A 组：K8s 调度器底层工程（6 题）

**A1. K8s Scheduler Framework 有哪些扩展点？你的 GPU 插件分别在哪些点实现？多插件打分如何聚合？**
> 考察点：工程落地深度。
> 答：PreEnqueue → PreFilter → Filter → PostFilter → PreScore → Score → NormalizeScore（打分归一化到 0-100，权重加权求和）→ Reserve → Permit → PreBind → Bind → PostBind。GPU 相关：Filter 做显存/型号/拓扑约束，Score 做 binpack 与成本，Reserve + Assume Pod 占位防并发冲突，Permit 做 Gang（等全组到齐才放行，超时则全部释放），Bind 阶段绑定具体 GPU UUID 并写 annotation。

**A2. 调度器为什么设计成 informer + SchedulingQueue + SchedulerCache 三层？ActiveQ / BackoffQ / UnschedulableQ 各自作用？调度失败的任务多久重试？**
> 考察点：是否被 K8s 语义真的吃透。
> 答：informer（list-watch + DeltaFIFO + Indexer）+ client-go workqueue 是标准控制器范式（解耦、去重、本地缓存降低 apiserver 压力，可对比 Kafka 消费者组做高吞吐削峰）。ActiveQ 是堆排序待调度队列；BackoffQ 是重试失败后指数退避等待（初始 1s，上限 10s，配合"podMaxBackoff"）；UnschedulableQ 是调度失败 Pod 的暂存区，事件驱动（MoveAllToActiveOrBackoffQueue）唤醒，定时器兜底（ unschedulableQTimeInterval 默认 60s 或 5min）。避免无意义热循环是关键设计。

**A3. K8s 如何避免两个 Pod 同时被调度到同一张 GPU？Assume Pod / Reserve 是干什么的？绑定冲突怎么处理？**
> 考察点：并发正确性。
> 答：Assume Pod 在 Bind 前把 Pod 先"假定"写入 SchedulerCache，使同一调度周期内后续 Pod 的打分能看到它占用；调用 AssumePod 时 informer 未确认前缓存中的 Pod 是 assumed 状态（未确认的 assumed pod 有 TTL，超时清除）。Reserve/Unreserve 保证两阶段（Permit/Bind）间资源不释放。K8s 是**乐观并发**：冲突在 Bind 阶段 API server 检查 nodeName 已被占/资源不足时失败，回退重来。GPU UUID 这种非 K8s 原生资源要自己做 compare-and-swap（借助 annotation/CRD 状态或 informer cache 内互斥）。

**A4. 调度器如何做高可用？多副本会不会重复调度？单副本如何保证性能？**
> 考察点：可靠性设计。
> 答：leader election（Lease 对象，configmap/lease/endpointslock）保证单活；多副本热备，主挂后秒到十几秒切换（LeaseDuration/RenewDeadline/RetryPeriod，默认 15/10/2s）。性能靠本地 cache（全量 memory 内调度）+ 并行 worker + 批量（percentageOfNodesToScore，默认在大集群只计算部分节点即停，平衡延迟与选型质量）+ 确定性重放（重启后从 API 重建状态，而非本地落盘 WAL）。

**A5. Gang scheduling 会不会死锁？多个 PodGroup 各占一部分资源互相等待怎么办？**
> 考察点：分布式死锁问题——是专家区分"用过"与"做过"的分水岭。
> 答：会。解法：(1) 超时回滚——Permit 等待超时则释放已分配部分回池；(2) 等待图检测；(3) 准入控制——资源不足以支撑最大 gang 时不接纳，避免"部署一半"；(4) 资源规整/槽位化，先从熵（碎片）层面降低死锁概率；(5) 严格优先级：高优 PodGroup 先拿，低优退让，避免环形等待。

**A6. Volcano / Yunikorn / Kueue 三者侧重点差异？什么场景选哪个？**
> 考察点：技术选型判断力。
> 答：Volcano——面向 HPC/AI 批处理，核心是 PodGroup + Gang + 丰富的 plugin（binpack/拓扑/ssh/DRF），CNCF 毕业项目，社区成熟；Yunikorn——源自 Hadoop/YuniKorn，核心是**层级队列 + 应用级配额 + 抢占**语义更强，支持 K8s+YARN 混合；Kueue——K8s 官方 Job Queueing 机制，做的是"准入排队"而非调度器本身，轻量、和原生 scheduler 配合，**多集群/配额联邦**友好。选型：自研 GPU 调度增强常 Volcano；需要企业多租户强配额 Yunikorn；只要排队与配额、不想自研调度器 Kueue。

---

### B 组：分布式训练与通信拓扑（6 题，最高优先级缺口）

**B1. 多机多卡训练为什么常有"木桶效应"？慢节点（straggler）怎么检测和处置？**
> 考察点：这是实际集群最大的痛点，面试官一问就能分辨真伪。
> 答：集合通信（AllReduce）是同步的，一个 node/k 慢，整个 step 慢。检测：每 step 耗时分布 + NCCL 超时日志 + 每节点 GPU 利用率同步性 + IB/ RoCE 端口丢包/重传/credit stall 计数 + Xid。处置：一级隔离（告警 + cordon），二级限制接新任务，三级自动 drain 并重新调度；常态化做法是"节点健康画像 + 亚健康预测"（对检测到温度/时钟降频趋势的节点提前迁移任务）。

**B2. AllReduce 有 Ring / Tree / NVLS 等实现，调度器如何根据并行策略（DP/TP/PP/EP）做放置决策？**
> 考察点：调度 × 通信协同——真正的专家不谈单点，而谈这条链路上的连锁影响。
> 答：TP（张量并行）通信量最大、延迟最敏感 → 必须同机、NVLink 域内、连续卡；PP 通信是点对点管道 → 相邻 stage 跨机需高带宽低延迟；DP 通信是梯度 AllReduce → 尽量同一 leaf switch/同一 pod 内，跨 spine 会打爆；EP（MoE 专家并行）是 all-to-all → 对网络对称性要求高。因此调度器需要**拓扑感知 + 并行策略标签**：同一任务的卡不只看数量，要看"通信域约束"。

**B3. IB / RoCE / GPUDirect RDMA / SHARP 有什么区别？多任务混跑时如何防止网络互相打爆？**
> 考察点：网络是 GPU 集群最容易出题的部分。
> 答：IB 原生无损（credit-based flow control）；RoCEv2 需靠 PFC + ECN + DCQCN 模拟无损，配置不当会出现"拥塞扩散"（incast），所以要做 PKEY 分区、区分 lossless 与 lossy 队列。GPUDirect RDMA 绕过 CPU 与主机内存，让显存直连网卡。SHARP 把 AllReduce 的聚合运算下推到交换机（NVIDIA 交换机方案），降低跨机流量（通常与 NCCL 配合使用）。多任务隔离：IB partition key 按 job 划分通信域、带宽 QoS 限速、cgroup net_cls/tc 做网络分类。

**B4. 大模型 MoE 架构采用专家并行（EP），调度上有什么新挑战？**
> 考察点：是否跟上 2025+ 最新技术。
> 答：专家并行 all-to-all 突发流量大，对**网络对称性**要求高；专家到卡的映射（placement）直接决定平均/尾延迟；通常要求"机内 NVLink + 机间 RDMA"分层放置，调度器需支持专家到卡的映射约束；此外 EP 对节点数敏感（专家数常被节点数整除、存在 2 的幂浪费），且存在"专家负载失衡"导致的 straggler。

**B5. 训练故障被打断，重新调度时拓扑还满足吗？如何解决"再次分配更少/不同节点"的问题？**
> 考察点：容错场景下的调度一致性。
> 答：靠 checkpoint 恢复 + 容错重构。(1) 优先按其原拓扑节点恢复（节点亲和 annotation 保留 preferred）；(2) 不满足时接受降级：min replicas vs 严格 gang 的取舍，PyTorch elastic agent / torchrun 的 rendezvous 支持可用节点数变化（world_size 变化时重启重分片），需要调度器发出伸缩信号与框架配合；(3) 预留 hot spare 节点用于快速替换。

**B6. 什么是弹性训练？调度器和训练框架如何联动？**
> 考察点：跨学科理解能力。
> 答：torchrun / Elastic Agent 支持动态增减 worker（rendezvous 重新协商 world_size，状态重分片）。调度器的角色：感知"弹性区间"（min/max replicas），资源紧张时回收超出 min 的部分（做 grow/shrink 而不是硬杀），资源宽松时回填扩展；回收要给 graceful 窗口（等待 checkpoint 写入）。这实际是"潮汐调度"在训练侧的实现。

---

### C 组：推理服务侧调度（5 题，强 AI Infra 岗必考）

**C1. LLM 推理有哪些部署形态（单卡 / TP / TP+PP / PD 分离）？分别对应什么拓扑调度要求？**
> 答：单卡——无拓扑约束，最灵活（适合小模型/低 QPS）；TP——同机 NVLink 域内连续卡，相邻通信；TP+PP——跨机时用 IB；PD 分离（prefill/decode 分开部署）——P 与 D 可以用**不同机型**（P 算力密集：H100/H800；D 显存带宽敏感：H20/L20/A10），且可以按 QPS 各自独立扩缩，是 2025 主流方向，调度器要支持"异构角色 + 比例调度"。

**C2. Continuous batching 与 Paged KV cache 对 GPU 显存规划有什么影响？**
> 答：KV cache 动态增长/释放 → 显存碎片（类似内存碎片），PagedAttention 用固定 block 缓解。这意味着：显存规划不能按"模型权重 + 固定 batch"算，而要峰值预留 + 水位控制（ watermark），超水位触发 preemption（swap/recompute）。调度器要知道"可用显存余量"才能安全混部。

**C3. 推理实例应该怎么扩缩容？GPU 调度如何与之联动？**
> 答：不按 GPU Util 扩（会误导），按业务信号：queue depth / request backlog / TTFT/TPOT P99 / KV cache 利用率。联动：扩容时调度器要**快**（优先级队列、预留热备池、镜像预热）；缩容时被移除的实例要有 graceful drain（等请求处理完）。

**C4. 多模型 / 多 LoRA 部署如何共享一张卡？**
> 答：小模型或 LoRA adapter 共享 —— MPS/Time-Slicing 聚合 + 显存池预留；如果显存能装下多个权重就用 time-slicing 隔离较差但简单；SLA 高的主模型独占。要用 serving framework 的 scheduler（vLLM/SGLang）而不是基础设施调度 —— 两层调度（平台层 + 引擎层）职责划分要清楚。

**C5. Prefill/Decode 分离架构下，如何确定 P/D 节点比例和调度策略？**
> 答：由 workload 特征（输入/输出 token 长度比、QPS）决定比例；KVCache 传输依赖 RDMA/高速网络；调度要支持按业务量分别独立扩缩 P 群与 D 群；实际中还会做"角色动态转换"（夜间把部分 Decode 副本转成 Prefill）。

---

### D 组：稳定性 / 故障 / 运维（4 题）

**D1. GPU 硬件健康管理怎么做？Xid / ECC / 掉卡 / 降频分别怎么处置？**
> 答：DCGM + dmesg + nvidia-smi 采集；分类处置：Xid 非致命（如 13/31/43/45/68/63/64/74/79/92/94/95）→ 标记 + 累计达阈值后 drain；ECC 行列 remap；掉卡（Xid 79 / PCIe 链路丢失）→ 立刻隔离并报修；降频/过热/功耗墙 → 亚健康标记，优先调度低优任务，不接长跑作业。目标：**坏而不瘫，自愈自动化**。

**D2. 一个跑了 3 周的大模型训练在第 18 天挂了，损失怎么算？怎么避免？**
> 答：Checkpoint 策略决定损失 = 平均成功间隔 × 重放成本。设计：checkpoint 频率与写入耗时平衡（异步/多级写入到本地+远端），自动中断-重提交，保留训练 infra 日志；跨 Fault Domain 打散 replicas（避免单个 rack 掉电全军覆没）。

**D3. 如何做"抢占式 / 低优先级资源"？怎么保证随时回收而业务可承受？**
> 答：平台侧标记 low-priority + 有限 grace period（收到抢占信号后在窗口内做 checkpoint/优雅退出）；明确告知用户"SLA 为 best-effort"；计费打折（竞价/闲时算力）；回收前发信号（SIGTERM + preStop），必要时按时间片分页回收一批节点。

**D4. 升级驱动 / CUDA / 固件怎么能不打断业务？**
> 答：滚动分批：逐个节点 drain → 升级 → 校验（跑 benchmark + 检查无 Xid）→ 放回集群；维护版本兼容矩阵；灰度顺序先自用任务后业务任务；采用容器化驱动方案（NVIDIA GPU Operator + driver container）减少对宿主机的强耦合。

---

### E 组：成本 / 产品化 / 技术选型（4 题）

**E1. 如何核算单个任务的 GPU 成本？怎么建立降本机制？**
> 答：单卡成本 = 折旧 + 电费 + 运维 + 网络/存储分摊，折算成"元/卡时"；任务成本 = 卡时 × 单价，且要把"申请但未用"的浪费部分也算进团队账上；把账目展示到团队和个人；配套机制是闲置配额回收 + 内部结算，让业务拥有决策自主权 → 形成"节省即收益"。

**E2. 为什么要用"业务产出 / 卡时"而不是"GPU 利用率"作为北极星指标？**
> 答：Util 是资源忙碌度，不反映价值产出；同一 30% Util 可能是满载低效也可能是空闲；产出/GPU-hour 直接关联 ROI，且天然引导优化两个方向（增产出 + 降卡时）。

**E3. 你理解的 AI 平台产品分层？GPU 调度在其中的位置？**
> 答：底层服务器与算力纳管 → 资源调度层（对应本岗位）→ 训练平台（任务管理、断点续训、实验管理）→ 模型服务平台（推理集群、灰度发布、自动扩缩）→ 上层业务应用。调度是**中间层的核心枢纽**：向下管理资源与成本，向上承诺 SLA；它是所有上层能力的底座。

**E4. Slurm 和 Kubernetes 之争：为什么很多大规模训练集群还在用 Slurm？迁移怎么考虑？**
> 答：Slurm 优势：HPC 生态成熟、作业语义简单、调度开销低、对大 gang 作业、拓扑亲和、MPI 支持好；K8s 优势：容器化生态（存储、网络、CI、多租户、自愈），适合在线+离线混部与多业务统一纳管。迁移：共存过渡（K8s 承接在线与新增任务，Slurm 管存量训练，中间打通账号/配额/作业视图），最终视团队技术栈与在线业务占比决定是否全面切换。

---

### F 组：算法与白板编码（4 题，容易被忽视但常考）

**F1. 写调度核心伪代码：给 N 个任务和 M 个节点，实现"过滤 + 打分 + 选优"，复杂度多少？怎么优化？**
> 答：O(N×M) 基础版本；优化：percentageOfNodesToScore 采样部分节点、并行 worker、堆维护 TopK、增量调度（只重算受影响节点）、BitMap 表示资源槽位加速过滤。

**F2. GPU 分配本质是多维装箱问题（NP-hard），业界用什么启发式？**
> 答：多维 bin packing 的 NP-hard 说明 + First Fit Decreasing / Best Fit Decreasing + 拓扑对齐合并 + 多维度（卡数+显存+CPU内存）归一化；在打分模型里以 score 形式落地，而不是求最优。

**F3. 有一批有依赖关系的 GPU 任务（DAG），如何排序使总完成时间最短？**
> 答：List Scheduling / HEFT（Heterogeneous Earliest Finish Time）：计算向上 ranking 值（关键路径），按 rank 降序插入，每个任务选最早完成时间（EFT）的处理器；复杂度 O(n²·p)。

**F4. 实现 Max-Min Fairness 分配**（白板常考题）
> 答：按"倒水"模拟：资源像水一样逐步均分给各参与者，先满足需求小的一方并冻结其份额，剩余资源在需求大者之间继续均分，迭代至资源耗尽。实现为排序 + 单轮遍历即可（O(n log n)），也可写两重循环的 O(n²) 版本。它是各类公平调度（含 DRF 多维扩展）的数学基础。

---

### G 组：STAR / 行为深挖（3 题）

**G1. 讲一个你做过最复杂的调度/性能优化项目：初始状态、你的角色、关键方案、量化收益、最后悔的决策？**
> 考察：自我反思能力。务必准备：基线数字 vs 优化后数字的对比、你的独特贡献（而不是笼统的"我们团队"）、以及走过哪些失败分支。

**G2. 你和业务团队起过什么冲突？（如：不愿意接受资源回收/使用率优化）怎么解决？**
> 考察：影响力与协作。考核三件套：数据说话 + 小范围试点 + 机制杠杆（计费/配额）。

**G3. 你设计过一个后来被证明有问题的系统决策吗？怎么发现的、怎么迭代？**
> 考察：成长性与诚实度。选真实的、复杂度高但后果可控的例子。

---

## 四、一周复习优先级建议

| 优先级 | 内容 | 投入 |
|---|---|---|
| P0 | 27 题主题库（尤其 Q5/8/9/17/20/22）— 已有文档 | 熟到能闭卷口述框架 |
| P0 | B 组（分布式训练与通信）+ C1（推理部署形态）— 最大缺口 | 每个概念能说清"为什么" |
| P1 | A 组 K8s 调度器底层（A1/A3/A5 最易被问到） | 手绘架构图 + 口述扩展点顺序 |
| P1 | F 组白板题（F1/F4 必练一遍） | 手写伪代码 |
| P2 | D 组稳定性 + E 组成本/选型 | 各准备 1 个真实/可信案例 |
| P2 | G 组 STAR | 提前写成文字，反复演练修订 |

---

## 五、一句话判断标准

**如果你的目标是"通过技术面"**——把 27 题主答 + B/C/A/F 补强看完，覆盖率足够（约 85%）。
**如果你的目标是"拿下专家岗 Offer"**——还必须拥有：**一次真实的线上事故复盘**、**一组自己经手的可量化收益数字**、**对未来方向（GB200/SuperPod、PD 分离、集群弹性上云）的判断**。这三样是主题库完全覆盖不到、但专家级面试必定用来区分候选人的判断点。
