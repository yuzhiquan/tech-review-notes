# GPU 调度研发工程师/专家 · 27 题全解析

> 来源：9 张面试题库图片（已获 Offer 候选人真实复盘 + 全网面经精华整理）
> 结构：内容总结 → Q5 完整示范解析（题库自带）→ Q1–Q27 逐题核心答案 + 发散延伸

---

## 一、题库内容总结

这套题库围绕 **GPU 集群调度系统** 展开，27 道题可归为 6 个考察维度：

| 维度 | 题号 | 考察重点 |
|---|---|---|
| 架构设计 | 1、2、4 | 从零设计 GPU 调度系统、K8s 扩展点、万卡规模性能 |
| 异构资源抽象 | 5、6、7 | 多卡型（A100/H100/L20/A10）抽象、算力换算、多维资源评分 |
| 公平与多租户 | 8、9、10、24 | 队列/配额/抢占、公平性、SLA 与防饿死、租户隔离 |
| 利用率优化与碎片治理 | 11–14、18 | Binpack、细粒度切分、拓扑碎片、短任务抖动、目标拆解 |
| 可观测与根因定位 | 15、16、19、20 | 利用率低的归因、指标体系、混部适配性判断 |
| GPU 共享与虚拟化 | 17、21–23、25–27 | 混部隔离、MIG/MPS/Time-Slicing 选型、K8s 虚拟资源表达 |

**出题规律**：几乎全部以"如果…你会如何…"的情景题形式出现，考察的不是背概念，而是 **建模能力（把硬件抽象为数据结构）→ 决策能力（在约束下做权衡）→ 落地能力（K8s/调度器层面的具体实现）→ 运营能力（指标、推动、成本）** 的完整链路。

---

## 二、Q5 完整示范解析（题库自带精华，作为其余各题的答题范式）

### 1. 考察意图
- **核心考察**：对异构计算资源的建模能力，能否通过软件抽象屏蔽硬件差异、提供统一用户接口。
- **隐藏考察（面试官真正想听的）**："标准化的资源描述语言"和"基于能力的调度"——你能否设计一套体系，让用户既能指定具体型号，也能只描述性能需求（算力、显存），由调度器自动匹配最合适的硬件。

### 2. 回答公式
**多维属性建模 → 资源标签化与分级 → 智能匹配算法 → 成本与效率权衡**

逻辑展开：定义包含算力、显存、架构版本、互联带宽的多维资源向量 → 通过标签系统对物理设备分类 → 设计调度器匹配逻辑（硬约束与软偏好）→ 阐述如何引导用户合理使用不同型号资源。

### 3. 优秀回答示例（完整版）

"面对异构的硬件环境，我会设计一套**基于能力画像**的资源抽象体系，而不是简单的型号字符串匹配。

**第一，建立多维资源向量模型。** 将每种型号的 GPU 抽象为一组标准参数：架构代际（安培/霍珀）、单精度/半精度算力峰值、显存容量与带宽、是否支持硬件切分。调度器看到的不再是 'A100' 这个名字，而是一个性能特征向量。我会定义'资源类'的概念，将性能相近的型号归为一类，方便用户提交任务时选择。

**第二，实施标签化与约束引擎设计。** 为每个节点自动打上多维标签（硬件型号、驱动版本、互联拓扑）。调度时支持两种模式：'精确匹配'模式下用户直接指定型号；'模糊匹配'模式下用户只声明最低显存和算力要求。调度器根据'资源价值权重'决策，例如优先将显存需求大但算力需求低的任务分配给显存充足但核心较弱的型号，留出高性能卡给重型训练任务。

**第三，引入动态成本因子。** 不同型号 GPU 采购和运行成本差异巨大，给每种资源设定虚拟'成本权重'。调度评分阶段，如果任务可以在低端卡上运行，调度器通过加权评分引导任务流向低端资源，实现'高低搭配'，避免用最贵的卡跑最简单的预处理任务。

**最后，针对特定硬件特性做深度适配。** 支持硬件切分的型号自动触发切分逻辑以支持多任务并行；具备高速互联能力的型号优先分配给需要大规模通信的分布式训练作业。通过这种抽象，不仅屏蔽了硬件差异，还实现了资源利用的最优解。"

### 4. 该回答的方法论亮点
- **抽象能力**：将具体硬件型号转化为"多维资源向量"，系统架构师的高阶思维
- **业务导向**：引入"成本因子"和"高低搭配"，体现对企业运营成本的关注
- **细节把控**：考虑硬件切分和互联特性，说明对硬件差异有深入研究
- **用户友好**：提供"精确"与"模糊"两种匹配模式，兼顾灵活性与易用性

### 5. 千万别踩的雷区
- **雷区一：硬编码判断逻辑。** 在代码里写大量判断语句区分型号（"如果型号等于 A100 就怎么做，等于 H100 就怎么做"）——每新增一种型号都要改代码，系统难以维护。
- **雷区二：忽略硬件间代际差异。** 简单认为显存够就能跑，不考虑算力核心的架构兼容性（"显存大于 24GB，A10 和 A100 没区别"）——导致任务运行效率低下甚至报错。

---

## 三、Q1–Q27 逐题回答与发散

> 每题结构：**核心答案**（面试主答，2–4 分钟口径）+ **发散**（加分延伸/追问预判）

---

### Q1. 从零设计一个面向 AI 训练和推理的 GPU 调度系统，整体架构如何设计？

**核心答案**：分层设计——
1. **接入层**：作业提交 API/CLI，统一任务描述（镜像、资源需求、副本数、拓扑约束、优先级）；
2. **任务管理层**：CRD + Controller 管理任务生命周期，实现 Gang Scheduling（训练任务全组原子调度）、重试、失败恢复；
3. **调度层**：队列/配额/优先级管理 + 调度器核心（过滤 → 打分 → 绑定），支持抢占与拓扑感知（NVLink/IB 域对齐）；
4. **资源层**：K8s + Device Plugin 上报 GPU，节点 agent 管理 GPU 分配/回收/隔离；
5. **数据感知**：对接存储，数据本地性调度（训练数据在哪个存储域附近就往哪调度）；
6. **可观测层**：DCGM 指标采集、任务 profiling、利用率与排队分析。

**发散**：
- 对比方案：直接用 Volcano/Yunikorn 二次开发 vs 自研调度器，各自 trade-off；
- 训练与推理调度目标不同：训练追求吞吐和 gang 原子性，推理追求延迟和弹性扩缩容，是否分池或混部是核心架构决策；
- 追问预判："调度循环延迟多少？""如何保证调度器自身高可用？"（多副本 + leader 选举 + 无锁快照）。

---

### Q2. K8s 原生调度能力不满足 GPU 任务需求，会在哪些环节做扩展？

**核心答案**：四个环节——
1. **资源上报**：自定义 Device Plugin 暴露 GPU 及其拓扑、健康状态；
2. **调度框架**：基于 Scheduler Framework 扩展点实现插件——Filter（显存/型号/拓扑约束）、Score（binpack/spread/成本）、Reserve（资源预留防竞争）、Permit（Gang 全员到达才放行）、Bind（绑定并打 GPU UUID）；
3. **API 层**：CRD（如 Volcano PodGroup）表达成组语义，配额用 ElasticQuota 表达 min/max；
4. **节点执行层**：扩展 device plugin / 自研 agent 实现 MIG 切分、vGPU、显存隔离。

**发散**：
- Scheduler Framework 插件 vs 完全自研调度器：前者生态兼容、迭代快，后者可做全局优化（批量调度、前瞻分配）；
- 追问预判："Permit 等待超时怎么处理？"（释放占位避免死锁）、"如何与 upstream K8s 版本解耦？"

---

### Q3. 集群同时存在训练、推理服务、离线批处理，如何设计调度优先级？

**核心答案**：
1. **分级**：在线推理（延迟敏感，SLA 最高）> 交互式开发/调试 > 离线训练 > 批处理/预处理；
2. **机制**：PriorityClass 分层 + 高优先级可抢占低优先级，但只回收"超发部分"（借出的弹性资源）；
3. **保证底线**：每个队列配 min guarantee（保证额度），抢占只能打这条线以上，低优任务不会被饿死；
4. **潮汐混部**：白天保在线，夜间低谷把算力让给训练/批处理，早高峰前自动驱逐回缩。

**发散**：
- 抢占粒度：整卡驱逐 vs MPS/MIG 粒度回收；训练任务被抢占后 checkpoint 恢复成本高，所以训练通常只抢占"弹性借用量"而非保证量（参考 YARN Capacity Scheduler 的 used-vs-guaranteed、阿里 GPU 分时混部实践）；
- 追问预判："推理自身流量波动大，怎么避免它抢占过猛？"（按 QPS 分级弹性配额，超发有上限）。

---

### Q4. 设计支持万卡级 GPU 集群的调度器，避免规模扩大后调度性能急剧下降？

**核心答案**：
1. **状态缓存**：调度器维护集群快照（informer cache），调度循环内不做 etcd/API 查询；用乐观并发处理冲突，失败重试；
2. **并行调度**：多 worker 并行调度互不冲突的任务（按节点分片或按资源域分区），或采用悲观锁只锁涉及的节点子集；
3. **分区/分层**：集群按资源池/机群分区，每区独立调度器 + 全局协调器（参考 Borg/Omega 的 shared state 思路）；
4. **批量与异步**：一个调度周期批量处理多个 Pod，bind 异步化；预热/优化优先队列（堆而非全排序）；
5. **增量计算**：只重算受事件影响的节点，不全局重刷。

**发散**：
- 量化口径：目标单次调度循环 P99 < 100ms、调度吞吐 > 千 Pod/s；
- 万卡的真正难点不在调度算法而在**故障域**：节点故障率上升、gang 任务部分失败重调度的雪崩效应，需要 backlog 重调度限流；
- 追问预判："乐观并冲突突率高怎么办？"（按资源热度分区、hot node hashing）。

---

### Q5. 同时支持 A100/H100/L20/A10 等多种 GPU 型号，如何做资源抽象和调度匹配？

**核心答案**：见上文第二节完整示范（多维资源向量 → 标签化与约束引擎 → 动态成本因子 → 硬件特性深度适配）。

**发散**：
- 补充技术落地：标签可对应 K8s node label + nodeSelector/affinity；"模糊匹配"可翻译成扩展资源 + 自定义 Filter 插件；
- 追问预判："H100 上跑为安培编译的 CUDA 内核会怎样？"（PTX 前向兼容但 SASS 不向后兼容，需重编译——正是"代际差异"雷区的具体体现）；"成本因子怎么标定？"（按采购价折旧 + 功耗电费折算成每卡时成本）。

---

### Q6. 业务方只提交任务所需算力、不关心卡型，如何设计资源规格转换和卡型选择？

**核心答案**：
1. **定义标准算力单位**：以参考卡的 TFLOPS 为基准换算"等效算力"，或直接用任务特征（模型大小、batch、预期时长）匹配；
2. **建立能力画像 + 基准库**：对每类卡跑标准 benchmark（训练吞吐、推理延迟），得到"任务类型 × 卡型"性能矩阵；
3. **选择策略**：默认按"满足需求下成本最低"选卡；考虑兼容性（架构代际、是否需要 NVLink）、可用性和队列等待时间；
4. **反馈修正**：任务实际吞吐回灌基准库，持续校准性能预测。

**发散**：
- 进阶做法：用回归模型/性能预测服务预估任务在每类卡上的吞吐，把"选卡"变成一个带成本约束的优化问题；
- 追问预判："用户声明的算力需求不准怎么办？"（先按保守估计分配，运行时 profiling 后支持热调整或下一轮修正）。

---

### Q7. 调度器同时考虑 GPU、CPU、内存、网络、存储，如何建立多维资源评分模型？

**核心答案**：
1. **向量化**：节点和任务都表示为资源向量 ⟨GPU, 显存, CPU, 内存, 带宽, 存储 IOPS⟩；
2. **归一化**：各维度按集群总量归一到 [0,1]，消除量纲；
3. **打分组合**：过滤（任一维度不满足即淘汰）→ 多个 Score 插件加权求和（binpack 分、均衡分、拓扑分、成本分、亲和分），权重可按业务域配置；
4. **关键权衡**：训练任务给"通信拓扑分"高权重（NVLink/IB 对齐），推理任务给"延迟/亲和分"高权重，离线任务给"binpack 分"高权重。

**发散**：
- 多资源公平分配可引用 **DRF（Dominant Resource Fairness）**；
- 追问预判："权重怎么定？"（初期专家经验 + 线上 A/B 调参，进阶用多目标优化或强化学习调权）；"存储如何感知？"（数据本地性打分：数据所在存储域优先）。

---

### Q8. 如何设计 GPU 调度系统中的队列、配额、优先级和抢占机制？

**核心答案**：
1. **队列**：层级队列树（组织 → 团队 → 业务），每个队列绑定调度策略（严格优先/加权轮询/fair-share）；
2. **配额**：min（保证额度，可超卖共享）+ max（上限），支持分时配额（白天/夜间不同模板）；
3. **优先级**：队列内任务级优先级 + 队列间权重，两者解耦；任务级优先级影响排序，不直接绕过队列配额；
4. **抢占**：高优队列在低于保证额度时可回收其他队列"超发"资源；被抢占任务按代价最小化选择 victim（优先选可 checkpoint、优先级最低、运行时间最短的）；预留机制防止抢占后资源又被别人抢走（reserve one）。

**发散**：
- Volcano 的 Queue/Pyqueue + ElasticQuota、YARN Capacity Scheduler 是成熟参照；
- 追问预判："抢占风暴怎么防？"（抢占动作限流、victim 冷却期、欠条 credit 让被抢占方下次优先归还）。

---

### Q9. 多个业务团队共享同一个 GPU 集群，如何保证资源公平性？

**核心答案**：
1. **配额保底**：每个团队有保证额度（min），空闲时允许其他团队借用；
2. **公平回收**：资源紧张时按"超过保证额度的部分"比例回收（如 DRF 或加权公平），保证量内的任务不动；
3. **欠条机制**：A 借了 B 的资源，B 需要时优先归还（credit/reclaim）；
4. **可解释性**：所有分配决策留痕，团队之间能看到各自"申请量/使用量/保底量/超发量"，公平是有据可查的。

**发散**：
- 单维 GPU 数公平 vs DRF 多维主导资源公平：如果有人囤显存、有人囤卡，DRF 更合理；
- 追问预判："团队虚报需求怎么办？"（利用率考核 + 配额定期校准 + 按实际使用计费，超额部分内部成本分摊）。

---

### Q10. 核心业务任务必须在高峰期优先运行，如何设计既保证 SLA 又避免长期饿死低优任务？

**核心答案**：
1. **SLA 层**：核心任务高优先级 + 资源预留池（高峰期专属配额，低峰期放开共享）；
2. **防饿死层**：
   - **Aging**：低优任务等待时间越长权重越高，最终必然被调度；
   - **欠条（credit）**：被抢占的任务获得欠条，资源释放时优先偿还；
   - **保底配额**：任何低优队列都有最小资源保障，抢占不能击穿；
3. **限流保护**：高优任务突发量大时排队缓冲，不无限抢占。

**发散**：
- 衡量饿死：监控队列"最长等待时间"和"公平份额达成率"作为 SLO；
- 追问预判："预留池平时闲置，利用率怎么办？"（低峰期允许低优任务进入，高高峰前通过驱逐窗口有序退出——即分时混部）。

---

### Q11. K8s 默认 Spread 策略导致 GPU 资源碎片，如何设计 Binpack 策略优化整体利用率？

**核心答案**：
1. **Score 插件**：实现 binpack 打分——节点剩余资源越少（放进去后碎片越小）得分越高，即 best-fit；
2. **多资源统一碎片度量**：不能只看卡数，用"放完该任务后各维度的最小剩余/最大剩余比"衡量碎片率；
3. **拓扑紧凑**：多卡任务优先在节点内紧凑排布（对齐 NVLink/NUMA），节点内放不下再跨节点按拓扑距离排序；
4. **碎片整理**：descheduler 识别低密度节点，迁移/驱逐小任务腾出整卡给大任务。

**发散**：
- binpack 与高可用是天然矛盾：全挤一台机器故障半径大，生产上一般"离线任务 binpack、在线任务适度 spread"；
- 追问预判："binpack 导致排队延迟上升怎么办？"（阈值控制：碎片率低于阈值才触发紧凑摆放）。

---

### Q12. 大量任务只需要半张卡或少量显存，如何避免整卡分配造成浪费？

**核心答案**：按隔离强度递进——
1. **MIG 硬件切分**（A100/H100/L20）：显存和算力物理隔离，适合多租户与生产推理；
2. **vGPU/驱动级切分**（如 HAMi、qGPU）：显存硬限制 + 算力软限制，兼容性广；
3. **Time-Slicing / MPS**：时间片轮转或算力合并共享，适合低优先级/开发测试；
4. **调度层配合**：device plugin 上报切分后的细粒度资源，调度器按"显存 + 算力份额"二维匹配，小任务聚合到同卡。

**发散**：
- 关键 trade-off：隔离越强灵活性越差（MIG 重配置需排空）；显存超卖风险与保护机制（见 Q26）；
- 追问预判："哪些任务不能切分？"（需要 NVLink 的多卡训练、受合规限制的独占任务）。

---

### Q13. 集群中大量 1/2/4/8 卡任务混部，如何减少 GPU 拓扑碎片？

**核心答案**：
1. **拓扑建模**：把节点内 GPU 拓扑（NVLink 域、PCIe/NUMA 亲和）建成图，2 卡任务必须落在同 NVLink 域，4 卡任务对齐 NUMA；
2. **槽位化分配**：按 8 卡节点划分标准槽位（8/4/2/1 卡槽），任务按槽位图分配，避免"东一张西一张"；
3. **Gang + 拓扑联合约束**：调度器保证成组任务的拓扑一致性，放不下就整组等待而非打散；
4. **重排**：定期 descheduler 整理，把小任务聚合到部分节点，腾出完整 8 卡节点给大任务。

**发散**：
- 量化指标：拓扑碎片率 = 因拓扑不满足而无法放置的空闲卡比例；
- 追问预判："大任务优先还是小任务优先？"（大任务先预留槽位可减少碎片但伤害小任务延迟，一般按队列配比分池解决）。

---

### Q14. 用户频繁提交短任务，导致 GPU 空洞和调度抖动，如何优化？

**核心答案**：
1. **短任务专用池**：按预估时长分队列，短任务走 MIG/时间片小卡池，避免占整卡；
2. **快速通道**：短任务简化审批/调度流程（跳过 gang 等待、免预留），提升调度吞吐；
3. **平滑与限流**：对单用户提交速率限流，突发走排队而不是冲击调度器；
4. **空洞回收**：短任务结束释放的卡延迟回收/立即填充下一任务（配合 binpack），或者用"任务水位预取"（预热镜像、预拉数据降低启动空洞）。

**发散**：
- 空洞的本质是"释放与填充的时间差"，优化方向是缩短任务冷启动（镜像缓存、数据本地性、checkpoint 复用）；
- 追问预判："调度抖动如何量化？"（单位时间 pod 重调度/驱逐次数、任务排队时间方差）。

---

### Q15. 如何判断 GPU 利用率低是调度问题、模型问题、数据加载问题还是网络通信问题？

**核心答案**：分层归因，看"卡在哪个环节"——
1. **先看指标特征**：SM 活动率低 + 显存带宽低 + kernel 执行时间占比低 → 大概率不是计算瓶颈；
2. **数据加载问题**：CPU util 高 / dataloader 等待长 / GPU 呈周期性锯齿（算一阵停一阵）；验证法：缓存整个数据集到内存后利用率明显上升；
3. **模型问题**：小 batch、算子串行依赖、CPU 算子多，GPU kernel 太碎；验证法：profiler（Nsight/PyTorch profiler）看 kernel 间隙；
4. **通信问题**：分布式训练中 NCCL 时间占比高、单节点慢（straggler）、IB 重传；验证法：对比卡间梯度同步等待、查 NVLink/IB 计数器；
5. **调度问题**：任务分到远端节点（数据跨域拉取）、GPU 被共享互相干扰、调度等待穿插在训练里。

**发散**：
- 关键认知：`nvidia-smi` 的 Util 是"采样期内有 kernel 在跑的时间占比"，不代表计算强度——可能挂着低利用率 kernel 也有 100%（呼应 Q20）；
- 追问预判："给我一套标准排查 SOP"（DCGM 全量指标 → PyTorch profiler → NCCL debug → 网络计数器，按序收敛）。

---

### Q16. 某业务 GPU 利用率长期只有 20%，业务方认为已满负载运行，如何定位和推动优化？

**核心答案**：
1. **定位**：用 profiling 数据代替口水仗——出示 SM 活动率、显存带宽、kernel 时间占比、每卡时产出（samples/s）四类指标，与同集群健康任务对比；
2. **归因**：区分"真满载但效率低"（模型/代码问题）和"假满载"（数据/通信等待）；
3. **方案**：给出具体优化清单——加大 batch、混合精度、算子融合、dataloader 并行、换合适卡型（用 A10 替代 A100 跑轻任务）；
4. **推动**：先做试点（帮业务方优化一个任务拿到收益）→ 建立利用率/产出效率的团队级考核与公示 → 引入按实际有效利用计费的成本分摊机制，让低效有代价。

**发散**：
- 推动优化的本质是**机制设计**：技术证据 + 标杆案例 + 利益挂钩三件套；
- 追问预判："业务方说指标不准怎么办？"（以任务产出吞吐而非卡利用率为准绳，双方认可口径先行）。

---

### Q17. 希望把在线推理服务和离线训练任务混部到同一批 GPU 上，如何设计隔离与弹性策略？

**核心答案**：
1. **优先级分层**：在线推理高优（SLA 保护），离线训练低优（弹性填谷）；
2. **隔离设计**：
   - 显存：MIG 硬隔离或 vGPU 显存上限，防止离线任务 OOM 冲垮在线；
   - 算力：MPS 算力配比限制离线任务份额，或 cgroup/时间片控制；
   - 故障：MIG 提供故障隔离域，一个租户 Xid 不影响另一个；
3. **弹性策略**：在线低谷（夜间/流量低点）把空闲卡借给训练；早高峰前设"驱逐窗口"，按 checkpoint 间隔有序驱逐训练任务；
4. **兜底**：在线 QPS 突增时秒级抢占（杀低优 + 释放显存），配合在线服务的 HPA 快速扩容。

**发散**：
- 收益来源：GPU 潮汐空闲（在线白天高、训练夜间高，天然互补）；
- 追问预判："混部后在线服务 P99 抖动怎么兜底？"（先灰度 5% 卡混部，设延迟回归自动熔断回滚）；可提阿里"在离线混部 GPU 分时复用"实践。

---

### Q18. 要把 GPU 利用率从 30% 提升到 60%，会从哪些维度拆解目标？

**核心答案**：按"水从哪里来"拆成四个可量化维度——
1. **碎片治理**（贡献约 5–10pt）：binpack、槽位化、descheduler 重排，降低碎片率；
2. **细粒度共享**（10–15pt）：小任务 MIG/vGPU 切分，把整卡空转的显存型任务聚起来；
3. **在离线混部**（10–15pt）：潮汐调度填充在线低谷空闲卡；
4. **任务效率**（5–10pt）：低效任务 profiling 优化、卡型降配（贵卡跑轻任务 → 高低搭配）；
5. **机制保障**：配额回收（长期占而不用）、计费改革（按有效利用计费）、北极星指标（集群整体有效利用率 + 每卡时产出）按月追踪。

**发散**：
- 强调方法论：先建可信基线（利用率的口径本身要先修正，见 Q20），再拆维度立里程碑，每季度复盘归因；
- 追问预判："哪个维度先做？"（ROI 排序：碎片治理和配额回收成本最低、见效最快，混部投入大但天花板高）。

---

### Q19. GPU 显存占用高但 SM 利用率低，如何判断是否适合做任务混部？

**核心答案**：
1. **判断任务画像**：显存高 + SM 低 → "显存型任务"（大模型推理常驻 KV cache 但算力波动），与"算力型任务"（训练，SM 高显存稳）互补性强，是理想混部搭档；
2. **看波动时序**：拉长时间线看 SM 利用率分布——长期 <20% 且方差小（真空闲算力）适合混入；脉冲型（忽高忽低）要评估干扰；
3. **隔离可行性**：显存型任务占的显存无法让出，混入任务只能用剩余显存 → 剩余显存是否够、是否支持 MPS 算力配比；
4. **干扰验证**：先在测试卡上做"混部压测"，量化双方性能退化（在线 P99 增幅、离线吞吐降幅），在 SLO 内才上线。

**发散**：
- 典型场景：LLM 推理服务（显存 90%+，SM 均值 20–30%）混批处理/离线训练，是业界提升利用率的主力手段；
- 追问预判："推理显存会随 batch 涨，怎么防 OOM？"（为在线预留显存 watermark，混部任务只允许用 watermark 之外部分）。

---

### Q20. 如何设计 GPU 集群利用率指标体系，避免只看单一 GPU Util 产生误判？

**核心答案**：四层指标——
1. **强度层（修正 Util 的缺陷）**：SM 活动率 + **SM 占用率（occupancy）** + **显存带宽利用率** + kernel 实际执行时间占比。Util 只是"有 kernel 在跑的时间占比"，一个 5% 强度的 kernel 挂着也能显示 100% Util；
2. **产出层（北极星）**：每卡时业务产出——训练 samples/s、推理 tokens/s 或 QPS×时延达标率，把"资源指标"升级为"价值指标"；
3. **效率层**：分配率 vs 实际使用率之差（申请未用）、排队时长、任务等待/运行比；
4. **健康层**：碎片率、拓扑碎片、Xid 错误率、混部干扰系数、回收率。

**发散**：
- 落地建议：DCGM 采集 + Prometheus 存储，按"集群 → 队列 → 任务"三级聚合，报表同时展示 Util 和有效利用率两列，训练大家看对的数字；
- 追问预判："有效利用率怎么定义？"（如 min(SM 活动, 显存带宽利用率) 或 kernel 加权强度，关键是被业务方认可并可复核）。

---

### Q21. 在 A100/H100 上使用 MIG，如何设计调度策略？

**核心答案**：
1. **Profile 管理**：按业务需求预定义切分方案（如 A100 分 7×1g.20gb 或 3×3g.40gb），每种方案作为一类可调度的"虚拟资源池"；
2. **上报与表达**：device plugin 将 MIG 设备以 UUID + profile 标签上报，K8s 侧作为 extended resources 调度；
3. **匹配策略**：任务声明最小显存/算力档位 → 匹配最小满足的 MIG 档（避免浪费大切片）；优先复用已切分卡，减少重切分；
4. **重配置控制**：MIG 重切需要排空该卡上的任务，策略上把重配置动作限制在低峰期 + 变更窗口，并做迁移预案；
5. **混合池**：同节点可保留部分整卡 + 部分 MIG 池，按任务标签路由。

**发散**：
- MIG 的代价：切片间无 NVLink、重配置慢 → 不适合需要卡间高速互联的训练；
- 追问预判："MIG 池和整卡池比例怎么定？"（按历史任务粒度分布动态调整，白天空余整卡夜间重切给小任务——分时重切）。

---

### Q22. MIG、MPS、Time-Slicing 三种 GPU 共享方式分别适合什么场景？

**核心答案**：

| 维度 | MIG | MPS | Time-Slicing |
|---|---|---|---|
| 隔离级别 | 硬件级（显存+算力+故障隔离） | 进程级（算力配比，显存不隔离） | 无（仅时间片轮转） |
| 显存隔离 | ✅ 物理隔离 | ❌ 共享、可超用 | ❌ 共享 |
| 算力隔离 | ✅ 固定切片 | ✅ 百分比限制 | ❌ 先到先得 |
| 故障隔离 | ✅ 强 | 一般 | ❌ 一个进程 Xid 全卡受累 |
| QPS 弹性 | 差（切分固定） | 好（配比可调） | 好 |
| 硬件要求 | Ampere+（A100/H100/L20） | 同代架构支持 | 任意 |

**场景结论**：
- **MIG**：多租户生产推理、合规隔离要求高、显存需求稳定的场景；
- **MPS**：同租户下多进程推理聚合、需要算力 QoS 但要求弹性的场景；
- **Time-Slicing**：开发测试、低优任务填充、对干扰不敏感的场景。

**发散**：
- 追问预判："MPS 服务器本身挂了怎么办？"（进程级单点，需节点 agent 监控重启）；"为什么 Time-Slicing 不适合生产推理？"（一个任务长 kernel 会把其他任务的延迟放大一个时间片）。

---

### Q23. 推理任务显存占用稳定但 QPS 波动明显，选 MIG、MPS 还是 Time-Slicing？

**核心答案**：**首选 MPS（或 MPS + 显存水位预留）**。
- 需求特征：显存稳定（可用 MIG/MPS 管显存），但**算力需求随 QPS 波动**，需要弹性；
- MIG 切分固定，QPS 高峰时算力天花板被切死，低谷时又浪费 → 与弹性诉求矛盾；重切分要排空，跟不上波动节奏；
- MPS 允许设算力百分比并可动态调整：低峰可让共存任务多跑，高峰收紧给推理让路；
- Time-Slicing 可作为降级方案（无 QoS），但延迟波动不可控。

**发散**：
- 组合拳：显存用 vGPU 上限管，算力用 MPS 百分比管，再叠加 HPA 按业务指标水平扩容；
- 追问预判："QPS 波动幅度极大怎么办？"（多副本 + 弹性池比单卡混部更根本，共享只解决小粒度问题）。

---

### Q24. 多个租户共享同一张 GPU，如何避免一个租户的异常任务影响其他租户？

**核心答案**：按影响面逐层设防——
1. **显存保护**：MIG 物理隔离（最强）或 vGPU 显存硬上限，防止一个租户超用显存把别人 OOM；
2. **算力保护**：MPS 百分比限制，防止一个任务打满 SM 饿死他人；
3. **故障隔离**：优先用 MIG——Xid 错误只影响本切片（这是 MIG 独有优势）；无法用 MIG 时，节点 agent 监控 Xid/挂死进程，自动隔离并重启异常进程，标记坏卡；
4. **行为限制**：cgroup 限制 CPU/IO、禁止租户执行需要特权驱动操作的代码（防止 `nvidia-smi --gpu-reset` 类操作）；
5. **事后**：异常租户熔断（暂停其任务）、事件留痕追责。

**发散**：
- 现实难点：非 MIG 共享下 ECC 错误和 Xid 是全卡范围的，这是"隔离 vs 利用率"权衡的核心依据；
- 追问预判："驱动级漏洞导致越权显存读取怎么办？"（多租户强隔离场景明确建议 MIG，共享方案只在可信租户间使用）。

---

### Q25. 支持GPU 细粒度切分，如何在 Kubernetes 中表达和管理这些虚拟资源？

**核心答案**：
1. **资源表达**：
   - 方案 A：Extended Resources——device plugin 上报 `nvidia.com/mig-1g.10gb: N`，Pod request 对应数量，原生亲和调度；
   - 方案 B：自定义资源维度——如 HAMi 风格 `vgpu/memory: 4000`，显存作为独立可调度维度，支持超卖策略；
   - 方案 C：**DRA（Dynamic Resource Allocation）**——K8s 新一代设备管理，用 ResourceClaim 描述设备需求（含属性约束），是未来方向；
2. **调度管理**：扩展 Filter/Score 匹配虚拟资源与拓扑；Bind 阶段完成具体 UUID 绑定并写回 Pod annotation；
3. **生命周期**：device plugin/节点 agent 负责实际 MIG 切分、恢复（节点重启后重建切分方案）、健康上报；
4. **配额与核算**：quota 按虚拟资源维度设置，监控按 UUID 粒度采集。

**发散**：
- 关键设计点：**调度与绑定的原子性**（防止两个调度循环绑同一 UUID）；
- 追问预判："DRA 相比 device plugin 解决了什么？"（结构化设备属性、声明式分配、支持更复杂的拓扑约束）。

---

### Q26. 虚拟 GPU 资源发生过度分配（超卖），如何设计保护机制？

**核心答案**：超卖的收益是提高利用率，风险是显存 OOM 和算力饥饿，保护设计四道防线——
1. **准入防线**：admission check 阶段评估节点已超卖比例，超过阈值拒绝新任务；在线/高优任务禁止落在超卖卡上；
2. **硬限制防线**：显存用 vGPU 驱动硬上限或 MIG 物理隔离——超卖算力可以，超卖显存要非常谨慎；
3. **运行时防线**：节点 agent 监控显存压力，接近 OOM 时按优先级驱逐低优任务；算力饥饿时收紧 MPS 配比；
4. **恢复防线**：被驱逐任务自动重排（带欠条优先恢复），事件上报 + 超卖策略自动回退。

**发散**：
- 超卖率分级：低优离线任务可超卖 1.2–1.5×，生产推理基本不超卖显存、轻超卖算力；
- 追问预判："超卖后 SLA 违约怎么算？"（超卖策略本身要带 SLA 承诺分级，低优任务超卖即接受'尽力而为'）。

---

### Q27. 某些模型对延迟非常敏感，是否应该与其他任务共享 GPU？如何判断？

**核心答案**：用"隔离下的 SLO 达标性"作为唯一判断标准——
1. **先量化基线**：独占卡时 P50/P99/P999 延迟；
2. **隔离方案测试**：在 MIG 切片上跑（显存/算力/故障硬隔离，理论上无干扰），对比延迟分布——若 P99 劣化 < SLO 余量（如 5%），可以共享且优先 MIG；
3. **软共享测试**：MPS/Time-Slicing 下做干扰压测，模拟最坏邻居（打满型任务共存），若 P99 仍达标可接受软共享；
4. **判断规则**：延迟 SLO 余量小（P99 与 P50 接近、尾延迟敏感）→ 独占或 MIG 硬隔离；余量大 → 可软共享。推理均值延迟敏感型任务在时间片共享下尾延迟会被放大，基本不推荐。

**发散**：
- 更高阶答案：把"延迟敏感度"做成任务标签进入调度器，敏感任务自动路由到独占/MIG 池， insensitive 任务填谷；
- 追问预判："共享收益很大但压测 P99 略超标怎么办？"（收益归业务、风险归架构，此时给出混部灰度 + 自动熔断回滚的折中方案，而不是拍板二选一）。

---

## 四、整体答题心法（从题库提炼）

1. **先建模再设计**：把硬件/任务都抽象成结构化数据（资源向量、标签、画像），体现架构师思维，避免"if 型号 == A100"式硬编码；
2. **分层作答**：考察意图 → 框架（回答公式）→ 分点展开 → trade-off，不追求一次说完所有细节；
3. **每个方案都要有代价**：主动说出 binpack vs 高可用、共享 vs 隔离、抢占 vs 饿死的权衡，比只讲优点可信十倍；
4. **机制 > 技术**：利用率、公平性这类题，最终落点在指标口径、计费、配额校准等运营机制上，这是专家和工程师的分水岭；
5. **准备量化口径**：调度延迟、超卖率、碎片率、有效利用率等数字信手拈来，说服力立增。
